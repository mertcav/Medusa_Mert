{
  "$comment": "PASS (DOĞRU RED): terminated tenant'ta resume → REJECT (terminated TERMİNAL; çıkış yok; DB.md §9). İhlal yok; kapı geçer.",
  "name": "tp-pass-reject-terminated-terminal",
  "expect": "pass",
  "request_id": "req-010",
  "op": "resume",
  "actor_realm": "platform",
  "plane": "platform_control_plane",
  "actor_permissions": ["tenant:suspend"],
  "current_status": "terminated",
  "tenant_id": "t-003",
  "audit_emitted": true,
  "expected": {"terminal": "REJECT"}
}
