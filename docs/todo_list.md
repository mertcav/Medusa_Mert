# Proje Görev Listesi — Work Breakdown Structure (WBS)
## Enterprise Voice AI Agent Platform (chanteur)

> Kaynak: `docs/BRD.md` (v2.1) · `docs/SAD.md` (v1.1). Çelişkide dokümanlar esastır.
> Bu dosya yaşayan bir belgedir; görev tamamlandıkça kutu işaretlenir ve durum güncellenir.

### Nasıl okunur (lejant)
- **WBS ID:** Hiyerarşik numara (ör. `3.2.1`). Üst seviye = workstream/epic, alt seviye = görev/alt-görev.
- **Faz:** `F0` (Temel/Foundation), `F1` (Core Voice Runtime & Pilot), `F2` (Enterprise Contact Centre), `F3` (Hyperscale & Regüle).
- **Öncelik:** MoSCoW — `Must` / `Should` / `Could`.
- **İz:** İlgili BRD FR/NFR/ADR referansı (izlenebilirlik).
- `[ ]` açık · `[~]` devam ediyor · `[x]` tamamlandı · `[!]` bloke/karar bekliyor.

### Faz kapıları (üst seviye plan)
| Faz | Hedef | Çıkış kapısı (gate) |
|-----|-------|----------------------|
| F0 | Proje, doküman, ortam, CI/CD, vendor eval, PoC | PoC gecikme+density doğrulandı (NFR 10.1/10.2); ADR-009 kararı verildi |
| F1 | Inbound core voice runtime + pilot (100–250 eş zamanlı) | BRD §19 kabul kriterleri (inbound altküme) geçti; 1 pilot müşteri canlı |
| F2 | Multi-tenant enterprise contact centre + outbound (1.000–3.000) | SSO/SCIM, outbound consent, CC entegrasyonu, dedicated/residency |
| F3 | Hyperscale active-active (10.000+), PCI, regüle modüller | Multi-region active-active, PCI ödeme, BYOK, marketplace |

### Açık kararlar (bunlar netleşmeden bağımlı görevler bloke)
- `[x]` **ADR-009** — Medya işleme edge'de mi merkezde mi? (density/gecikme dengesi) → **Kabul: hibrit** (edge VAD/endpointing+barge-in per ADR-005 + ayrı/eş-konumsuz medya katmanı). 0.3.4 deneyiyle kapatıldı.
- `[!]` **ADR-010** — Self-hosted LLM + GPU kapsamı → F3.
- `[!]` BRD §22 açık kararları (ilk ülke/sektör, pilot use-case, telekom sağlayıcı, ilk diller, retention süreleri, PCI kapsamı, ilk CC entegrasyonu).

---

## 0. Program Yönetimi & Temel Altyapı (Foundation)

### 0.1 Doküman ve gereksinim hattı
- [x] **0.1.1** SRS (System Requirements Specification) yaz — FR/NFR'leri test edilebilir sistem gereksinimlerine indir — `F0` · `Must` · →BRD §24 · ⇒ `docs/SRS.md` v1.0 (236 SR, 193/193 FR + NFR 10.1–10.7 kapsandı)
- [x] **0.1.2** Tam izlenebilirlik matrisi (FR ↔ SRS ↔ test case ↔ WBS) oluştur — `F0` · `Must` · ⇒ `docs/RTM.md` v1.0 (193 FR + 7 NFR → 236 SR → 236 TC → 168 WBS; FR→SR %100, FR→WBS %93,8; üretici `docs/gen_rtm.py`)
- [x] **0.1.3** Veri Tabanı tasarım dokümanı (BRD §16 varlıkları: PK/FK, indeks, RLS politikaları) — `F0` · `Must` · →BRD §16 · ⇒ `docs/DB.md` v1.0 (28 varlık → tablo + PK/FK + indeks + RLS; tenancy sınıfı, partition, residency/KMS, WORM audit, retention/legal-hold)
- [x] **0.1.4** API tasarım dokümanı (OpenAPI sözleşmeleri + adapter SPI sözleşmeleri) — `F0` · `Must` · →SAD §8.1, §14.4 · ⇒ `docs/API.md` v1.0 (5 yüzey: L0/L1/L2 + Public Developer API OpenAPI 3.1 + Adapter SPI; ortak konvansiyonlar RFC 9457/idempotency/pagination; webhook event kataloğu + imzalama/retry; STT/TTS/LLM/Telephony SPI + ortak yetenekler + hata taksonomisi + fallback; permission-key→endpoint eşlemesi; YAML/JSON örnekleri parse-doğrulandı)
- [x] **0.1.5** Threat model (STRIDE) ve güvenlik tasarım derinleştirme — `F0` · `Must` · →SAD §24 · ⇒ `docs/THREAT_MODEL.md` v1.0 (14 varlık + 9 güven sınırı TB-1..9 + DFD; STRIDE kataloğu TM-S/T/R/I/D/E ~45 tehdit, Likelihood×Impact derecelendirme; 20 güvenlik kontrol derinleştirme SEC-01..20; LLM/voice-özgü tehditler OWASP LLM Top-10 hizalı; önerilen ADR-014..017; FR/SR/SAD izlenebilirlik + test eşlemesi)
- [x] **0.1.6** DPIA şablonu + compliance profile parametre seti (TR/UK/EU/ME) — `F0` · `Must` · →BRD §14 · ⇒ `docs/DPIA.md` v1.0 (controller/processor rol ayrımı; DPIA tetik kriterleri DPIA-T-01..08 + onboarding gate; önceden-doldurulmuş DPIA şablonu §D1..§D9; config-driven compliance profile parametre modeli `cp.*` 8 alan; PROFILE-TR/UK/EU/ME ülke profilleri + ME composite/alt-ülke notu; sektörel overlay PCI/FCA/HIPAA/NHS/SOC2/ISO/DORA/NIS2; most-restrictive-wins çözümleme motoru; enforcement noktaları + FR/SR/SAD izlenebilirlik + test eşlemesi; tüm FR/SR/TC çapraz-referansları doğrulandı)
- [x] **0.1.7** `.docx` üretim hattı (pandoc + branded reference-doc) operasyonelleştir — `F0` · `Should` · →docs/build-docx.sh · ⇒ `build-docx.sh` (8 dokümanı otomatik keşfeder/üretir: `all`/`list`/alt-küme, `--regen-ref`/`--no-toc`, pandoc kontrolü + özet) + `docs/make_reference_docx.py` (RMC markalı reference-doc üreteci: accent1 `#1F4E79`, header/footer+sayfa no, tr-TR; stdlib-only) + `docs/assets/rmc-reference.docx` üretildi; 8/8 doküman uyarısız üretildi, çıktı XML doğrulandı
- [x] **0.1.8** ADR kayıt süreci ve repo'da ADR klasörü disiplinini kur — `F0` · `Should` · ⇒ `docs/adr/` (süreç+disiplin): `README.md` (ne zaman ADR, `ADR-NNNN` numaralandırma, durum yaşam döngüsü, oluşturma akışı), `0000-template.md` (MADR-uyarlı TR şablon: bağlam→sürücüler→seçenekler→karar→sonuçlar→izlenebilirlik), `gen_adr_index.py` (stdlib-only üreteç: `--next`/`--list`/`--check`; README + `SAD.md §22` indeks tablolarını işaretçiler arası türetir), ADR-001..017 backfill (SAD §22'deki 13 + THREAT_MODEL ADR-014..017); `SAD §22` türetilen indekse çevrildi, CLAUDE.md doküman yapısı + THREAT_MODEL atfı güncellendi; `--check` 17/17 güncel

### 0.2 Vendor değerlendirme (PoC'li)
- [x] **0.2.1** Telekom sağlayıcı eval (Twilio/Telnyx/SIP) — medya streaming gecikmesi — `F0` · `Must` · →ADR-002 · ⇒ `docs/vendor-eval/` (sağlayıcı-nötr eval hattı): `telephony-media-latency.md` (3 entegrasyon modu M1 managed-WS/M2 ham-SIP-RTP/M3 CC; 8 ölçütlü ağırlıklı rubrik, C1 taşıma gecikmesi knock-out kapısı; SAD §20 medya bütçesi tek-yön P95 ≤100ms / yeşil ≤50ms) + `media_latency_probe.py` (stdlib-only; minimal RFC6455 WS; `serve`/`probe`/`stats`/`compare`; 8kHz/20ms 50fps frame-RTT, jitter, loss, E-model MOS-vekil, bütçe gate→exit kodu) + `samples/` (illüstratif profiller, ≥2 managed aday). Self-test+senaryo+stats+compare doğrulandı; nihai seçim → 0.2.6 (vendor-neutral)
- [x] **0.2.2** STT eval (≥2 sağlayıcı): streaming partial/final, confidence, dil (EN+TR), 8kHz — `F0` · `Must` · →FR-STT-001 · ⇒ `docs/vendor-eval/` (sağlayıcı-nötr STT eval): `stt-eval.md` (SttAdapter SPI yüzeyi; 8 ölçütlü ağırlıklı rubrik C1 final-gecikme/C2 WER/C3 yapısal-CER knock-out kapıları + C4 partial-final/C5 confidence/C6 dil/C7 phrase-boost/C8 8kHz-residency-no-train; metrik tanımları WER en-kötü-dil EN+TR + CER yapısal alan; kapılar final P95 ≤200ms SAD §20 / WER ≤0.15 / CER ≤0.05) + `stt_eval_probe.py` (stdlib-only; `score`/`compare`/`selftest`; Levenshtein WER/CER TR-duyarlı küçük harf, final-flush gecikme P50/P95/P99, partial/final yapı kontrolü FR-STT-002, confidence kapsama+ayrım+ECE kalibrasyon FR-STT-006/007, kapı→çıkış kodu) + `samples/stt-cloud-A/B/C-degraded.json` (illüstratif ≥2 aday + 1 elenen profil). 17/17 selftest + 3 sağlayıcı score + compare doğrulandı; A→SARI(pass), B/C→KIRMIZI(fail) kapı elemesi gösterildi; nihai seçim → 0.2.6 (vendor-neutral)
- [x] **0.2.3** TTS eval (≥2 sağlayıcı): first-byte gecikme, barge-in kesme, ses kalitesi — `F0` · `Must` · →FR-TTS-001/002 · ⇒ `docs/vendor-eval/` (sağlayıcı-nötr TTS eval): `tts-eval.md` (TtsAdapter SPI yüzeyi; 8 ölçütlü ağırlıklı rubrik C1 first-byte/C2 barge-in-kesme/C3 MOS/C4 ölü-hava knock-out kapıları + C5 telaffuz/C6 ses-tutarlılığı/C7 cache/C8 8kHz-residency-no-train; metrik tanımları MOS en-kötü-dil EN+TR + barge-in cancel→stop + dead-air underrun/RTF; kapılar first-byte P95 ≤200ms SAD §20 / barge-in P95 ≤200ms NFR 10.1 / MOS ≥4.0 en-kötü-dil / underrun=0) + `tts_eval_probe.py` (stdlib-only; `score`/`compare`/`selftest`; TTFB/barge-in P50/P95/P99 percentile, MOS en-kötü-dil FR-TTS-001/003, dead-air underrun+RTF FR-RES-009, telaffuz oranı FR-TTS-004, cache-hit ayrımı FR-TTS-010, kapı→çıkış kodu) + `samples/tts-cloud-A/B/C-degraded.json` (illüstratif ≥2 aday + 1 elenen profil). 19/19 selftest + 3 sağlayıcı score + compare doğrulandı; A→SARI(pass), B→KIRMIZI(TR MOS 3.6 en-kötü-dil eler), C→KIRMIZI(TTFB 342ms + barge-in 393ms + ölü hava) kapı elemesi gösterildi; nihai seçim → 0.2.6 (vendor-neutral)
- [x] **0.2.4** LLM eval (≥2 sağlayıcı + küçük/büyük tier): first-token, "no-train" endpoint, bölgesel — `F0` · `Must` · →FR-LLM-001/012 · ⇒ `docs/vendor-eval/` (sağlayıcı-nötr LLM eval): `llm-eval.md` (LlmAdapter SPI yüzeyi; 8 ölçütlü ağırlıklı rubrik C1 first-token-genel/C2 küçük-tier-first-token/C3 kalite-en-kötü-dil/C4 no-train/C5 bölgesel/C6 stall knock-out kapıları + C7 tool-call soft + C8 ≥2-tier/fallback; metrik tanımları TTFT genel+küçük/büyük tier ayrı, kalite en-kötü-dil EN+TR, no-train=noTrain+NONE/EPHEMERAL, bölgesel=region pinning, stall=inter-token boşluk; kapılar TTFT genel P95 ≤400ms SAD §20 / küçük-tier P95 ≤200ms FR-LLM-013 / kalite ≥0.85 en-kötü-dil / no-train+bölgesel+stall=0 zorunlu) + `llm_eval_probe.py` (stdlib-only; `score`/`compare`/`selftest`; TTFT genel+per-tier P50/P95/P99, kalite en-kötü-dil, küçük-tier payı raporu SR-DEN-005, no-train/no-log + bölgesel uygunluk, inter-token stall FR-LLM-004, tool-call oranı FR-LLM-008, kapı→çıkış kodu) + `samples/llm-cloud-A/B/C-degraded.json` (illüstratif ≥2 aday + 1 elenen profil). 27/27 selftest + 3 sağlayıcı score + compare doğrulandı; A→SARI(pass), B→KIRMIZI(TR kalite 0.815 en-kötü-dil eler), C→KIRMIZI(bölgesel yok + no-train kapalı + TTFT 511ms + stall) kapı elemesi gösterildi; nihai seçim → 0.2.6 (vendor-neutral)
- [x] **0.2.5** Vector DB eval (pgvector vs OpenSearch) — `F0` · `Should` · →SAD §12.1 · ⇒ `docs/vendor-eval/` (sağlayıcı-nötr Vector DB eval): `vector-db-eval.md` (VectorDbAdapter yüzeyi; RAG retrieval — medya hot-path değil, SAD §10.2/§20 RAG kalemi; 8 ölçütlü ağırlıklı rubrik C1 recall-en-kötü-sorgu-sınıfı/C2 retrieval-gecikme/C3 namespace-tenant-izolasyon/C4 metadata-ACL-filtre/C5 residency-no-log knock-out kapıları + C6 index-ops/C7 hybrid-search/C8 operasyonel-stack-fit soft; metrik tanımları recall@k=retrieved∩relevant exact-kNN ground-truth en-kötü sorgu sınıfı + cross-tenant/ACL forbidden-leak + residency pinning+no-log; kapılar recall@k ≥0.95 en-kötü sınıf / retrieval P95 ≤200ms SAD §20 / cross-tenant+ACL sızıntı=0 FR-KB-004/005+FR-TEN-002 / residency+no-log NFR 10.7+FR-KB-010) + `vector_db_eval_probe.py` (stdlib-only; `score`/`compare`/`selftest`; recall@k retrieved∩relevant, retrieval P50/P95/P99 genel+sınıf, forbidden-leak cross-tenant/filtreli-ACL, residency uygunluk, kapı→çıkış kodu) + `samples/vdb-pgvector/opensearch/managed-degraded.json` (illüstratif ≥2 aday + 1 elenen profil). 28/28 selftest + 3 aday score + compare doğrulandı; pgvector→SARI(pass, 60ms yeşil-gecikme+native RLS) ve OpenSearch→SARI(pass, 120ms+native hybrid) iki meşru aday geçti, managed-degraded→KIRMIZI (residency yok + dış log + cross-tenant sızıntı + ACL ihlali + structured recall 0.35 + retrieval 286ms) çoklu kapı elemesi gösterildi; nihai seçim → 0.2.6 (vendor-neutral)
- [x] **0.2.6** Vendor karar raporu + sözleşme/DPA/alt-işleyen listesi taslağı — `F0` · `Must` · →SAD §8.1/§8.4, ADR-002, BRD §14.1/§19 · ⇒ `docs/vendor-eval/` (0.2.x sentez/kapanış): `vendor-decision.md` (sağlayıcı-nötr karar çerçevesi: 0.2.1–0.2.5 teknik kapı roll-up + ticari/uyumluluk/risk boyutları D1–D5, D3 residency/no-train + D4 DPA/alt-işleyen **knock-out**; ADR-002 **portföy kapısı** = her hot-path kategoride ≥2 farklı geçen aday + fallback BRD §19 1–4; SAD §8.4 hipotezlerinden karar kayıtları — bağlayıcı seçim **provizyonel/BEKLEMEDE**, 0.3.x PoC + DPA imzasına bağlı) + `contract-dpa-checklist.md` (controller=tenant/processor=RMC/sub-processor=sağlayıcı rol modeli; RMC↔sağlayıcı C-01..12 + tenant↔RMC D-01..12 checklist `cp.*` eşlemeli; **alt-işleyen kayıt Ek-A** iskeleti; sağlayıcıya giden veri kaydı BRD §14.1) + `vendor_decision_probe.py` (stdlib-only; `rollup`/`register`/`selftest`; 0.2.x score/stats JSON → kategori çıkarımı + verdict normalizasyonu top-level/`gate.*` + portföy kapısı→çıkış kodu; Ek-A iskeleti config bayraklarından; **yeniden ölçmez, seçmez**). 16/16 selftest + uçtan uca rollup (illüstratif set: telekom 3 geçer ✅ + STT/TTS/LLM her biri 1 geçen → 🔴 portföy eksik kapı elemesi gösterildi, B adayları kasıtlı KIRMIZI) + register doğrulandı; nihai seçim **0.3.x'e** ertelendi (vendor-neutral)

### 0.3 PoC / Spike (mimari doğrulama)
- [x] **0.3.1** Uçtan uca tek inbound akış PoC (telefon→STT→LLM→TTS→telefon) — `F0` · `Must` · →SAD §25 · ⇒ `docs/poc/` (sağlayıcı-nötr E2E spike): `e2e-inbound-poc.md` (tasarım: PSTN→SBC→MediaGW→STT→Orchestrator(LLM)→TTS→MediaGW→PSTN akışı; SAD↔PoC mimari eşleme; turn state machine LISTEN→CAPTURE→THINK→ACT→SPEAK + barge-in; SAD §20 gecikme bütçesi kalemleri; 9 HARD invariant kapısı; kapsam ayrımı 0.3.2 gecikme/0.3.3 density/0.3.4 ADR-009/0.3.5 hot-path) + `e2e_inbound_poc.py` (stdlib-only; `run`/`selftest`/`schema`; SPI adapter seam ADR-001/002, sanal saat deterministik, edge VAD/endpointing+barge-in, STT primary→secondary fallback FR-STT-008, correlation_id+tenant context propagation SAD §13.3, AI disclosure karşılama BRD §14.2, session memory kalıcılaştırma, end-of-utterance→ilk-ses gecikme kırılımı, kapı→çıkış kodu) + `samples/inbound-happy-path/barge-in/stt-fallback.json` (sentetik senaryolar FR-TST-008) + `README.md`. 21/21 selftest + 3 senaryo uçtan uca 🟢 GEÇTİ (happy P50≈578ms/P95≈669ms illüstratif, ≤1200ms yeşil); gecikme **yumuşak** (formal P95 kapısı 0.3.2). Vendor seçmez; canlı PoC'ta aynı SPI'ye gerçek adapter takılır
- [x] **0.3.2** Gecikme bütçesi ölçümü (P50/P95/P99, barge-in kesme) — `F0` · `Must` · →NFR 10.1, SAD §20 · ⇒ `docs/poc/` (0.3.1'in **yumuşak** gecikmesini **HARD** kapıya çevirir): `latency-budget.md` (end-of-utterance→ilk ses metriği SAD §20 tam zincir endpointing dahil; lognormal bileşen modeli median+p95; tier/cache/rag/tool tur-mix; NFR 10.1 HARD kapı e2e P50≤700/P95≤1200/P99≤2000 + barge-in P95≤200 + tool overhead P95≤100; bileşen-içi SAD §20 bant yumuşak flag; bileşen atfı/headroom) + `latency_budget_probe.py` (stdlib-only; `measure`/`compare`/`selftest`/`schema`; tohumlu Monte-Carlo deterministik örnekleme, lognormal(p50,p95) kalibrasyon, P50/P95/P99 lineer-interp percentile 0.3.1 ile aynı, uç-uca + barge-in + tool dağılımları, bileşen atfı pay%/headroom, kapı→çıkış kodu) + `samples/latency-green-baseline/tail-heavy/red-degraded.json` (illüstratif profiller FR-TST-008). selftest GEÇTİ 11 grup/20 kontrol (lognormal median/p95 kalibrasyon ±%4, kapı geçer/eler, determinizm birebir, atıf payları≈%100, küçük-tier↑→P95↓); green→🟢 (P50 632/P95 863/P99 968ms, barge 149ms — tümü geçer), tail-heavy→🔴 (P50 695 geçer ama **P95 1242 eler** — kuyruk riski, median-bakışı yetersiz), red-degraded→🔴 (P50/P95/P99+barge-in+tool hepsi eler). Bulgu: bütçe **dar** — green'de bile ~%36 LLM first-token + ~%22 endpointing → 0.3.4 (medya konumu/ADR-009) + 0.3.5 (hot-path runtime) girdisi. Vendor-neutral; canlı PoC'ta aynı profil yapısı gerçek telemetri ile doldurulur
- [x] **0.3.3** Density ölçümü (oturum/worker, ~15MB/oturum) — `F0` · `Must` · →NFR 10.2, ADR-003 · ⇒ `docs/poc/` (0.3.2'nin disiplinini density'ye taşır — NFR 10.2'yi HARD kapıya çevirir): `density-measurement.md` (metrik: oturum-başı orkestratör belleği **medya HARİÇ** ≤15MB FR-RES-016 + worker density = **bellek ∩ CPU** kapasitesi ≥250–500; lognormal bileşen modeli median+p95 — session/dialogue/prompt/rag/adapter/runtime; mean-packing CLT-kapı + p95-packing muhafazakar; medya eş-konumu **duyarlılık** girdisi → 0.3.4/ADR-009; NFR 10.2 HARD kapı bütçe P95≤15MB + density≥250, stretch ≥500 yumuşak) + `density_probe.py` (stdlib-only; `measure`/`compare`/`selftest`/`schema`; Monte-Carlo tohumlu deterministik, `LogNormalComponent`+`percentile` 0.3.2 ile birebir, medya draw'ları **ayrı RNG stream** → bütçe medya konumundan bağımsız, bileşen atfı pay%, kapı→çıkış kodu) + `samples/density-green-baseline/media-colocated/red-degraded.json` (illüstratif profiller FR-TST-008). selftest 13/13 GEÇTİ (kalibrasyon/kapı/determinizm/medya-hariç invariant'ı/uzun-oturum↑→bellek↑); green→🟢 (P95 13.9MB ≤15, density 552 ≥500 stretch dahil, bottleneck=CPU), media-colocated→🟢 (bütçe değişmez 13.9MB ama density 552→355 → ≥250 geçer 500 ıskalar), red-degraded→🔴 (özetsiz geçmiş+thread-per-call → bütçe 35.5MB + density 158 ikisi de eler — ADR-003 ihlali). Bulgu: **density CPU-bağlı** (16GB worker'da 15MB bütçe ~1300 oturuma yer verir, tavanı CPU koyar) → 0.3.5 hot-path Go/Rust girdisi; medya konumu density'yi ~%35 keser → 0.3.4/ADR-009 girdisi. Vendor-neutral; canlı PoC'ta (0.3.5) aynı profil gerçek RSS/heap+CPU telemetri ile dolar
- [x] **0.3.4** Medya işleme konumu deneyi (edge vs merkez) → **ADR-009 kararı** — `F0` · `Must` · →ADR-009 · ⇒ `docs/poc/` (0.3.2 gecikme + 0.3.3 density HARD kapılarını tek karar deneyinde birleştirir): `media-placement.md` (edge/merkez/hibrit topolojileri; topolojiye-bağlı yalnız medya/ağ kalemi + barge-in kesme + medya eş-konumu değişir, `rest_of_chain` sabit; birleşik NFR 10.1+10.2 kapısı; ölçülen latency/bargein/density + beyan ops/cost/residency ağırlıklı karar matrisi toplam 1.0) + `media_placement_probe.py` (stdlib-only; `evaluate`/`compare`/`selftest`/`schema`; `LogNormalComponent`+`percentile` 0.3.1/0.3.2/0.3.3 ile birebir, tohumlu Monte-Carlo, e2e+barge-in+density birleşik kapı→çıkış kodu, HARD-geçen adaylar arası ADR-009 önerisi) + `samples/media-{edge,central,hybrid}.json` (illüstratif FR-TST-008). 12/12 selftest + 3 topoloji evaluate + compare doğrulandı: **merkez 🔴 barge-in P95≈266ms eler** (ADR-005'i doğrular) + density 554→354; **edge 🟢 geçer ama ops/cost/residency cezası** (skor 0.486); **hibrit 🟢 her iki kapı + en yüksek skor 0.605 → ADR-009 ÖNERİSİ**. **ADR-009 `Açık`→`Kabul` (hibrit)**; ADR indeks + SAD §6.4/§23 güncellendi. Vendor/dil-nötr; canlı PoC (0.3.5) aynı profil yapısını gerçek telemetri ile doldurur
- [?] **0.3.5** Hot-path dil doğrulaması (Go/Rust async runtime) — `F0` · `Must` · →ADR-003

### 0.4 Mühendislik altyapısı (platform engineering)
- [?] **0.4.1** Mono/poly-repo stratejisi + repo yapısı (data plane / control plane ayrı) — `F0` · `Must` · →ADR-004
- [?] **0.4.2** IaC (Terraform) — bölge, ağ (VPC/segmentasyon), private endpoint — `F0` · `Must` · →NFR 10.6
- [?] **0.4.3** Kubernetes cluster(lar) + namespace stratejisi — `F0` · `Must` · →SAD §16.1
- [?] **0.4.4** CI/CD hattı (build/test/lint/SAST/DAST/dependency scan/SBOM) — `F0` · `Must` · →NFR 10.6
- [?] **0.4.5** Secrets yönetimi (Vault/KMS) + BYOK iskeleti — `F0` · `Must` · →NFR 10.6
- [?] **0.4.6** Ortam matrisi: draft/test/staging/production — `F0` · `Must` · →FR-AGT-005
- [x] **0.4.7** Gözlemlenebilirlik omurgası (OTel + Prometheus + Grafana + Loki) iskeleti — `F0` · `Must` · →SAD §17 · ⇒ `docs/platform/observability/` (vendor-neutral, credential-free iskelet): `observability-spec.json` (makine-okunur kaynak doğruluk: 20 metrik BRD §15 + 15 span + 11 alarm SAD §17.2 + label/kardinalite politikası + sampling + 3 pipeline) + `observability-backbone.md` (tasarım: tek-giriş/üç-sinyal/çok-çıkış mimari, span modeli SAD §17.1, kardinalite disiplini, alarm kataloğu, izlenebilirlik) + `observability_probe.py` (stdlib-only doğrulayıcı: `validate`/`coverage`/`selftest`/`schema`; kapsam + kardinalite + alarm bütçesi ≤2dk NFR 10.1 + çapraz-tutarlılık → çıkış kodu) + `config/` (otel-collector.yaml tail-sampling+tenant-context+PII/kardinalite guard; prometheus.yml; alerts.yaml 11 kural `for:`≤2dk; loki.yaml multi-tenant; grafana voice-runtime panosu refresh≤60sn FR-ANA-012) + README. selftest 11/11 + validate 5 kapı 🟢 (çıkış 0) + coverage BRD §15/SAD §17.2 %100; bugfix: `$comment` meta-anahtarı dosya sanma → atla. F1 §14.1 temeli (vendor-neutral)
- [x] **0.4.8** Yük/performans test ortamı (call generator/synthetic load) — `F0` · `Should` · →FR-TST-006 · ⇒ `docs/platform/load-testing/` (vendor/dil-nötr, credential-free, sentetik FR-TST-008): `load-test-harness.md` (tasarım: sentetik çağrı üreteci↔SUT mimari eşleme; Little yasası kapasite + Poisson geliş + ramp/soak/burst profil; autoscale/warm pool FR-RES-013 + admission/backpressure FR-RES-014 graceful red; tıkanma f(util) yük-altı gecikme/bellek bozulma modeli; 6 HARD kapı; senaryolar) + `load_test_probe.py` (stdlib-only kesikli-zaman simülasyon `run`/`compare`/`selftest`/`schema`; tohumlu Poisson geliş + autoscale gecikmesi + admission muhasebeli red + yük-altı e2e gecikme [0.3.2 base × f(util)] + yük-altı çağrı-başı bellek [0.3.3 base × büyüme]; `LogNormalComponent`+`percentile` 0.3.2/0.3.3 ile birebir; kapı→çıkış kodu) + `scenarios/load-{pilot,enterprise,hyperscale,burst-2x,overload-degraded}.json` (illüstratif Faz hedefleri BRD §20). selftest 12/12 🟢 (Little/Poisson kalibrasyon, green hard_pass, red 6-kapı elemesi, determinizm, kapasite↑→red↓, congestion↑→gecikme↑); 4 senaryo 🟢 (hyperscale 100 CPS×10.000 steady: headroom %41.6, soak red %0, yük-altı P95 988ms, bellek P95 14.4MB, util 0.80 — **FR-TST-006/§19(11) karşılanabilir**) + burst-2x 🟢 (2x ani trafik warm pool+autoscale ile red %0 emildi, NFR 10.3) + degrade 🔴 (yetersiz workers_max + admission≥1.0 + yüksek tıkanma → 6/6 kapı eler). Bugfix/bulgu: 2x burst **mutlak region tavanında emilemez** (graceful shed) → 2x absorpsiyon `workers_max≥2×steady` kapasite planlaması girdisi (BRD §10.3); hyperscale steady tavan, burst absorpsiyonu izole `load-burst-2x`. İskelet kapısı; canlı yük testi F1 §18.5/§18.6'da gerçek trunk+telemetri ile dolar

---

## 1. Veri Mimarisi & Persistence

### 1.1 Şema ve depolar
- [x] **1.1.1** PostgreSQL şeması: Tenant, Org Unit, User, Role (+ RLS) — `F1` · `Must` · →BRD §16, SAD §13.1 · ⇒ `db/` (DB.md §5.1 tasarımının ilk fiziksel dilimi; araç-nötr SQL migration'lar, credential-free): `migrations/0001_extensions_helpers` (citext/pgcrypto + **`gen_uuid_v7()`** RFC 9562 UUIDv7 üreteci DB.md §3 + `set_updated_at`/`raise_immutable_violation` + **`app_rw`** RLS-bypass-etmeyen uygulama rolü DB.md §6.1) + `migrations/0002_tenant_org_iam` (7 tablo: tenant/organisation_unit/app_user/role/permission_key/role_permission/user_role_assignment; UUIDv7 PK, FK `ON DELETE RESTRICT`, FK indeksleri; sağlamlaştırma: app_user realm↔tenant_id CHECK FR-IAM-008 + platform e-posta partial-unique) + `migrations/0003_rls_tenant_org_iam` (**ENABLE+FORCE RLS** + politikalar: org_unit standart izolasyon, tenant Root self+platform-only-write FR-TEN-001, app_user/user_role_assignment karışık desen DB.md §6.3; **fail-closed** `current_setting('app.*',true)`; **WITH CHECK** cross-tenant write koruması; global rol tabloları RLS-dışı + app_rw'ye REVOKE ALL/GRANT SELECT) + `seeds/roles.sql` (12 sabit rol FR-IAM-011/ADR-012, idempotent) + `tests/rls_isolation.sql`+`run_live_test.sh` (canlı RLS davranış kapısı, CI/sunucu varsa; yoksa SKIP) + `schema_probe.py` (stdlib-only statik kapı: `validate`/`selftest`/`schema`). selftest 14/14 🟢 + validate **72/72** 🟢 (UUIDv7 sürüm/variant bitleri, tenant_id+RLS, fail-closed, WITH CHECK, BYPASSRLS-yok, global salt-okunur, FK indeks, down-drop, seed eşlemesi, DB.md izlenebilirlik). Canlı RLS testi F1 §0.4.4/§12.2.3'te gerçek sunucuyla koşar (DB.md §10). Vendor-neutral; her up/down ileri-yönlü+idempotent
- [x] **1.1.2** Agent, Agent Version, Prompt, Conversation Flow, Voice/Model/STT Profile şeması — `F1` · `Must` · →BRD §16 (5–11), SAD §13.1 · ⇒ `db/` (DB.md §5.2 tasarımının ikinci fiziksel dilimi; 1.1.1 deseniyle birebir, araç-nötr migration, credential-free): `migrations/0004_agent_config` (7 tablo: agent/agent_version/prompt/conversation_flow/voice_profile/model_profile/stt_profile; UUIDv7 PK, tenant_id NOT NULL FK `ON DELETE RESTRICT`, FK indeksleri, conversation_flow.graph **GIN**; **dairesel FK** agent.active_version_id→agent_version inline değil **ALTER** ile DB.md §10; **agent_version WORM** §6.5/FR-AGT-006: BEFORE UPDATE OR DELETE → raise_immutable_violation trigger) + `migrations/0005_rls_agent_config` (7 tablo saf tenant-scoped → **ENABLE+FORCE RLS** + standart `tenant_isolation` USING+WITH CHECK fail-closed; mutable config app_rw CRUD; **agent_version WORM grant** REVOKE ALL + yalnız GRANT SELECT,INSERT — UPDATE/DELETE yok) + `tests/agent_config_isolation.sql` (canlı RLS+WORM kapısı: izolasyon, cross-tenant write reddi, dairesel FK çözümü, agent_version UPDATE/DELETE reddi, platform realm iş-config görmez = altın kural; fail-closed için GUC **NULL** reset — `''::uuid` hata fırlatır) + `run_live_test.sh`/`schema_probe.py` genişletildi. selftest **20/20** 🟢 + validate **140/140** 🟢 (1.1.1 72 korundu + 1.1.2 68 eklendi: tablo/UUIDv7 PK, tenant_id NOT NULL, RLS+WITH CHECK, fail-closed, dairesel-FK-deferred, WORM trigger+grant, GIN, FK indeks, down-drop+FK-drop+policy, agent_version'a hiç UPDATE/DELETE grant yok, DB.md izlenebilirlik FR-AGT-001/003/006+FR-LLM-012). Canlı RLS+WORM testi F1 §0.4.4/§12.2.3'te gerçek sunucuyla koşar. Vendor-neutral; her up/down ileri-yönlü+idempotent
- [x] **1.1.3** Call, Call Leg, Transcript, Recording, Event, Tool Execution şeması — `F1` · `Must` · →BRD §16 (19–24), SAD §13.1 · ⇒ `db/` (DB.md §5.5/§7.2 tasarımının üçüncü fiziksel dilimi; yüksek hacim / partition'lı; 1.1.1/1.1.2 deseniyle birebir, araç-nötr migration, credential-free): `migrations/0006_call_interaction` (7 tablo: call/call_leg/transcript/transcript_segment/recording/call_event/tool_execution; tümü **RANGE (created_at) partition'lı** + `(id, created_at)` kompozit PK + UUIDv7 default; tenant_id NOT NULL **gerçek FK**, call→agent/agent_version gerçek FK; **call_id/transcript_id mantıksal FK** — partition'lı parent'a REFERENCES yok DB.md §7.2; campaign_id/tool_id mantıksal — tablolar 1.1.4'te, FK sonra ALTER; her tablo **DEFAULT partition** [insert güvenlik ağı] + **`create_month_partition()`** aylık yardımcı DB.md §7.2; **tool_execution WORM** §6.5/P4: BEFORE UPDATE OR DELETE trigger + idempotency UNIQUE (tenant_id, idempotency_key, created_at) FR-TOOL-009; call_event payload **GIN**, recording retention **partial index** WHERE legal_hold=false) + `migrations/0007_rls_call_interaction` (7 tablo saf tenant-scoped → **ENABLE+FORCE RLS** + standart `tenant_isolation` USING+WITH CHECK; mutable 6 tablo app_rw CRUD; **tool_execution WORM grant** REVOKE ALL + yalnız INSERT+SELECT) + `tests/call_interaction_isolation.sql` (canlı RLS+WORM+**partition** kapısı: izolasyon, partition routing `tableoid`→`call_p202606`, cross-tenant write reddi, idempotency duplicate reddi, tool_execution UPDATE/DELETE reddi, mutable call UPDATE geçer, platform realm transcript görmez = altın kural) + `run_live_test.sh`/`schema_probe.py` genişletildi. selftest **30/30** 🟢 + validate **240/240** 🟢 + **canlı PostgreSQL 16 ile uçtan uca 🟢** (3 test dosyası, up→test→down, re-runnable). **Bugfix (çapraz, güvenlik):** fail-closed politikalar `current_setting(...)::uuid` çıplak cast'ti; bağlantı havuzunda `SET LOCAL` sonrası placeholder **boş-string'e (`''`) döner, NULL değil** → çıplak `''::uuid` *hata* fırlatır (fail-error, fail-closed değil). Tüm politikalar **`NULLIF(current_setting(...),'')::uuid`** ile sertleştirildi (0003/0005/0007 + DB.md §6.2/§6.3/§6.4); havuz-unutulmuş-scope artık 0 satır (gerçek fail-closed), canlı doğrulandı. 1.1.1/1.1.2 canlı testleri ilk kez gerçek sunucuda koşturuldu (önce hep SKIP'ti) + GUC '' reset bug'ı düzeltildi. Vendor-neutral; her up/down ileri-yönlü; partition yönetimi operasyonel (DB.md §7.2)
- [x] **1.1.4** Campaign, Contact, Consent, Usage Record, Call Evaluation, Audit Log, Incident şeması — `F2` · `Must` · →BRD §16 (16–18, 25–28), SAD §13.1 · ⇒ `db/` (DB.md §5.4/§5.6 tasarımının dördüncü fiziksel dilimi; BRD §16'nın **28 varlığını tamamlar**; 1.1.1–1.1.3 deseniyle birebir, araç-nötr migration, credential-free): `migrations/0008_outbound_ops_governance` (7 tablo: campaign/contact/consent/call_evaluation [non-partition] + usage_record/audit_log [**RANGE created_at partition'lı** + `(id,created_at)` PK + UUIDv7 + DEFAULT partition]; tenant_id NOT NULL gerçek FK [saf tenant-scoped 5], tenant_id **NULL'lanabilir** [karışık audit_log/incident]; **audit_log FK YOK** [WORM bağımsızlık DB.md §5.6 — tenant_id/actor_user_id/break_glass_id plain UUID]; call_id mantıksal FK [partition'lı call'a REFERENCES yok §7.2]; **consent + audit_log WORM** §6.5/P4: BEFORE UPDATE OR DELETE → raise_immutable_violation trigger; **gecikmeli FK kapatma** — 0006'da mantıksal bırakılan `call.campaign_id` artık **`ALTER TABLE call ADD CONSTRAINT fk_call_campaign`** gerçek FK [partition'lı call→normal campaign, PG destekler]; contact DNC partial index, consent son-durum index) + `migrations/0009_rls_outbound_ops_governance` (5 standart `tenant_isolation` USING+WITH CHECK fail-closed + **2 karışık `tenant_or_platform`** [audit_log/incident: `tenant_id = NULLIF(...) OR (tenant_id IS NULL AND app.platform='on')` — tenant kendi satırı, platform realm yalnız tenant_id IS NULL → **altın kural** DB.md §6.3]; mutable 5 tablo app_rw CRUD; **consent/audit_log WORM grant** REVOKE ALL + yalnız INSERT+SELECT) + `tests/outbound_ops_governance_isolation.sql` (canlı RLS+WORM+partition+**karışık-politika** kapısı: fail-closed, izolasyon, partition routing `usage_record_p202606`/`audit_log_p202606`, yeni call→campaign FK reddi, cross-tenant write reddi, consent UPDATE + audit_log DELETE WORM reddi, mutable incident UPDATE geçer, tenant sahte-platform write reddi, platform realm yalnız platform satırı görür + tenant-spesifik yazamaz) + `run_live_test.sh`/`schema_probe.py` genişletildi. selftest **33/33** 🟢 + validate **327/327** 🟢 (240 korundu + 87 eklendi) + **canlı PostgreSQL 16.14 ile uçtan uca 🟢** (4 test dosyası, up→test→down, re-runnable, exit 0; 11/11 assertion). Yeni desenler: karışık platform+tenant RLS (`mixed_policy_ok` predikatı) + gecikmeli FK kapatma. Kapsam dışı (bilinçli): `break_glass_grant` + Tier B `break_glass_read` ek politikası → IAM/break-glass dilimi (1.1.3 deseniyle tutarlı). Vendor-neutral; her up/down ileri-yönlü; partition yönetimi operasyonel (DB.md §7.2)
- [x] **1.1.5** Redis (session memory, semantic/TTS cache index) kurulum + TTL politikaları — `F1` · `Must` · →SAD §12.1 · ⇒ `cache/` (SAD §12.1 "Cache" deposu → fiziksel keyspace+TTL sözleşmesi, `db/` disipliniyle aynı: araç-nötr/credential-free/statik+canlı kapı): `keyspace-spec.json` (makine-okunur kaynak doğruluk: 2 pool [session noeviction / cache volatile-lru] + 4 keyspace [session_state/summary + semantic_cache + tts_cache_index] + TTL/eviction/persistence/PII/residency + invariant I1..I9) + `config/redis-{session,cache}.conf` (persistence kapalı `save ""`+`appendonly no`, protected-mode, TLS/ACL yorum bloğu) + `config/users.acl.example` (default-off + en-az-yetki, parola `${ENV}`) + `redis_probe.py` (stdlib-only `validate`/`selftest`/`schema`) + `tests/cache_behavior_test.py` (minimal RESP socket istemcisi, bağımlılıksız) + `run_live_test.sh` (2 örnek up→test→teardown; redis yoksa SKIP). **İnvariant'lar:** I1 tenant hash-tag `{t:<tenant_id>}` namespace (SEC-15/TM-I-09), I2/I3 TTL zorunlu+sonlu (session idle 1800s/cap 14400s sliding · semantic 24h · tts 30g), I4 PII yalnız session (cache `pii=false`, TM-I-09), I5 session noeviction+delete_on_call_end / cache volatile-lru+evictable, I6 serbest-metin hash'li (ham PII anahtara yok), I7 residency home-region (NFR 10.7). selftest **23/23** + validate **52/52** 🟢; canlı (Redis 7.x, 2 örnek) **13/13** assertion 🟢 (pool politikaları runtime'da, TTL+expiry+kalıcı-anahtar-yok, tenant SCAN izolasyonu, delete_on_call_end, **cache evict eder vs session OOM-reddeder/sessiz-kayıp-yok**). Vendor-neutral (ADR-002); sır repoya yazılmadı. → `reports/1.1.5-redis-cache-ttl-raporu.md`
- [x] **1.1.6** Nesne depolama (S3-uyumlu) tenant-bazlı bucket/prefix + KMS + lifecycle — `F1` · `Must` · →SAD §12.1 · ⇒ `objstore/` (SAD §12.1 "Nesne depolama" deposu → fiziksel bucket/prefix+KMS+lifecycle sözleşmesi, `db/`+`cache/` disipliniyle aynı: araç-nötr/credential-free/statik+davranış kapısı): `storage-spec.json` (makine-okunur kaynak doğruluk: **per-tenant-per-region bucket** stratejisi + SSE-KMS/CMK/crypto-shred şifreleme + 5 data_class [recording/transcript/kb_document/tts_cache/export] prefix/PII/versioning/object-lock/lifecycle + access_model tenant/platform/break-glass + invariant O1..O11) + `policy/` (bucket-policy.template TLS-only+SSE-KMS+cross-tenant+object-lock-bypass reddi; public-access-block 4 bayrak; lifecycle.template 5 kural transition/expire/noncurrent/abort-multipart; iam-tenant-prefix-policy yalnız `${BUCKET}`+tenant CMK; object-lock-config governance WORM — hepsi `${ENV}`/`${PLACEHOLDER}`) + `objstore_probe.py` (stdlib-only `validate`/`decide`/`lifecycle`/`selftest`/`schema`; **deterministik access-decision simülatörü** bucket-policy+IAM+altın kural mantığı ALLOW/DENY + **lifecycle simülatörü** yaş+legal-hold→eylem, sunucu gerektirmez) + `tests/objstore_behavior_test.py` (canlı S3-uyumlu davranış kapısı, aws CLI; endpoint yoksa SKIP) + `run_live_test.sh` (yerel MinIO+aws varsa ayağa kaldırır; yoksa SKIP). **İnvariant'lar:** O1 per-tenant-per-region+residency home-region (NFR 10.7), O2 SSE-KMS+tenant CMK (NFR 10.6), O3 TLS-only (SAD §14.2), O4 public-access kapalı, O5 IAM scoped bucket+platform content deny-by-default (altın kural FR-IAM-008), O6 data_class prefix+ham PII yok (P6), O7 lifecycle+legal-hold muafiyeti (FR-REC-006/007/010), O8 PII→content, O9 crypto-shred (FR-REC-010, DB.md §9), O10 object-lock governance WORM (FR-REC-007), O11 literal sır yok. selftest **29/29** + validate **63/63** 🟢; tts_cache 30g WBS 1.1.5 tts_cache_index TTL ile hizalı; retention gün değerleri mühendislik varsayılanı (BRD §22 açık karar, `cp.retention.*` yalnız sıkılaştırır). Canlı S3 kapısı F1 §0.4.2/§0.4.5'te gerçek endpoint+KMS ile koşar. Vendor-neutral (ADR-002); sır repoya yazılmadı. → `reports/1.1.6-objstore-bucket-kms-lifecycle-raporu.md`
- [x] **1.1.7** Vector store (pgvector/OpenSearch) namespace (tenant+agent) — `F1` · `Must` · →FR-KB-004 · ⇒ `db/` (DB.md §5.3 tasarımının fiziksel dilimi — BRD §16 varlık 12–14; 1.1.1–1.1.4 deseniyle birebir, araç-nötr migration, credential-free): `migrations/0010_knowledge_base` (3 tablo: knowledge_base/kb_document/kb_chunk; UUIDv7 PK, tenant_id NOT NULL FK; **namespace** TEXT NOT NULL + `CHECK (length>0)` + UNIQUE (tenant_id, namespace) — tenant+agent retrieval kapsam anahtarı FR-KB-004; agent_id **nullable** FK [NULL => tenant-shared KB]; **kompozit tenant-kapsamlı FK** `(tenant_id, kb_id)`/`(tenant_id, document_id)` → parent `(tenant_id, id)` — FK doğrulaması RLS'i bypass ettiğinden cross-tenant kb_id bağını **yapısal olarak** imkânsız kılar [defense-in-depth, FR-KB-004/FR-TEN-002]; her parent UNIQUE (tenant_id, id) taşır; **pgvector koşullu**: `pg_extension` kontrolüyle varsa `kb_chunk.embedding VECTOR(1536)` + HNSW ANN `vector_cosine_ops` koşullu ALTER ile eklenir, yoksa kb_chunk **metadata-only** kalır [embedding harici depo / OpenSearch yolu DB.md §12 — vendor-neutral ADR-002]; kb_document ACL/TTL/sensitive [FR-KB-005/008/010] + content_ttl/sensitive partial indeks) + `migrations/0011_rls_knowledge_base` (3 tablo saf tenant-scoped → **ENABLE+FORCE RLS** + standart `tenant_isolation` USING+WITH CHECK fail-closed `NULLIF(current_setting(...),'')::uuid`; KB **WORM değil** → app_rw tam CRUD [re-ingest FR-KB-003 / staleness silme FR-KB-008]) + `tests/knowledge_base_isolation.sql` (canlı RLS + **namespace** + kompozit-FK kapısı: fail-closed, cross-tenant izolasyon, **cross-agent namespace izolasyonu** [X namespace yalnız X chunk'ı döndürür, Y sızmaz — FR-KB-004], cross-tenant write reddi, **cross-tenant kb_id chunk insert reddi** [kompozit FK], platform realm KB içeriği görmez = altın kural; chunk metadata-only → pgvector'sız da koşar) + `run_live_test.sh`/`schema_probe.py` genişletildi. selftest **33/33** 🟢 + validate **380/380** 🟢 (327 korundu + 53 eklendi: tablo/UUIDv7 PK, tenant_id NOT NULL FK, namespace NOT NULL/CHECK/UNIQUE, agent nullable FK, 3 kompozit tenant FK + parent UNIQUE(tenant_id,id), pgvector-koşullu embedding+HNSW + embedding base'de yok, RLS+WITH CHECK, fail-closed, app_rw CRUD + WORM-trigger-yok, TTL/sensitive partial indeks, down-drop+policy, DB.md izlenebilirlik FR-KB-004/005/008/010) + **canlı PostgreSQL 16 ile uçtan uca 🟢** (5 test dosyası, up→test→down, re-runnable, exit 0; pgvector kurulu değil → metadata-only NOTICE doğrulandı, KB 6/6 assertion geçti). Vendor-neutral (ADR-002); DB.md §5.3/§12 + db/README güncellendi (kompozit tenant FK + pgvector koşullu). → `reports/1.1.7-vector-store-namespace-raporu.md`
- [x] **1.1.8** Event stream (Kafka) topic tasarımı + replay politikası — `F1` · `Must` · →ADR-007 · ⇒ `eventstream/` (SAD §12.1 "Event stream" yeteneğinin → ADR-007 fiziksel sözleşmesi; `db/`+`cache/`+`objstore/` disipliniyle aynı: araç-nötr ADR-002 / credential-free / statik probe + deterministik davranış simülasyonu + opsiyonel canlı kapı): `topic-spec.json` (makine-okunur kaynak doğruluk: **13 topic** domain-odaklı `<domain>.<subject>.v<major>` + envelope sözleşmesi + cluster_defaults [RF=3/isr=2/acks=all/idempotent/unclean-leader-kapalı] + **global_replay_policy P1–P8** + 12 consumer group + invariant **I1–I15**) + `eventstream-topics.md` (tasarım: mimari konum [data plane→event stream→async tüketici], topic ilkeleri, **PII tiering** transcript.raw 24s-kısıtlı→redaction→transcript.redacted 30g, no-loss billing/audit, compacted config.snapshot, replay politikası, cross-tenant izolasyon residency+ACL+RLS, izlenebilirlik) + `eventstream_probe.py` (stdlib-only: `validate`/`replay`/`route`/`selftest`/`schema`; **deterministik replay simülatörü** at-least-once+idempotent dedup→çift-yan-etki-yok / canlı-offset-dokunulmaz / PII-gate-audit / poison→DLQ + **partition routing simülatörü** same-key→same-partition; kapı→çıkış kodu) + `schemas/event-envelope.schema.json` (API §10.1 webhook envelope üst kümesi; zorunlu id/idempotency_key/tenant_id/correlation_id/partition_key/pii_class…) + `config/` (topics.declarative.json spec-türevi provisioning manifesti I15 + acl.template.json least-privilege `${ENV}` + cluster-defaults.properties + schema-registry.config.json BACKWARD/FULL) + `tests/envelope_behavior_test.py` (T1–T4 envelope+webhook indirgemesi iç-alan-sızdırmaz) + `run_live_test.sh` (statik kapı + Kafka CLI/`${KAFKA_BOOTSTRAP_SERVERS}` varsa canlı topic uygula/doğrula; yoksa SKIP) + README. selftest **18/18** 🟢 + validate **236/236** 🟢 (I1–I15 + spec↔config + envelope + secret-tarayıcı) + replay **9/9** 🟢 (P1–P8) + route 🟢 + envelope test **6/6** 🟢. **Bugfix:** secret-tarayıcı `# sasl.jaas.config:…` **yorum** satırını literal-sır sandı (235/236) → yorum satırları (`#`/`//`) elendi (sırrı *tarif eden* açıklama ≠ sır). Kapsam dışı (bilinçli): OLAP sink şeması→1.1.9, redaction motoru impl.→ileri WBS, tiered/cold replay→1.1.6+1.1.9 bağı. Vendor-neutral (ADR-002); sır repoya yazılmadı. → `reports/1.1.8-event-stream-kafka-topic-replay-raporu.md`
- [x] **1.1.9** OLAP (ClickHouse/BigQuery) analitik şeması — `F2` · `Must` · →SAD §12.1 · ⇒ `analytics/` (SAD §12.1 "Analytics — kolon-bazlı OLAP" deposu → fiziksel yıldız modeli; `db/`+`cache/`+`objstore/`+`eventstream/` disipliniyle aynı: araç-nötr ADR-002 / credential-free / statik probe + deterministik sorgu simülasyonu + opsiyonel canlı kapı): `olap-spec.json` (makine-okunur kaynak doğruluk: **5 fact** [fct_call/fct_turn/fct_usage/fct_qa_evaluation/fct_campaign_daily] + **5 dimension** [dim_tenant/agent/campaign/provider/date] + **4 rollup MV** [mv_call_daily/turn_latency_daily/usage_daily/agent_version_perf] + engine_neutral type/partition/order/ttl/dedup eşlemesi + pii_policy + retention_policy katmanları + ingestion lineage + invariant **A1–A14**) + `olap-schema.md` (tasarım: mimari konum data plane→event stream→OLAP, sağlayıcı-nötrlük ClickHouse↔BigQuery eşlemesi, yıldız modeli, PII politikası, **4-katman tenant izolasyon**, idempotent ingest, gereksinim kapsama) + `olap_probe.py` (stdlib-only: `validate`/`query`/`selftest`/`schema`; **deterministik OLAP sorgu simülatörü** — predicate'siz sorgu fail-closed reddi + cross-tenant sızıntı=0 + rollup agregasyon doğruluğu elle-hesap birebir + PII projeksiyon reddi + at-least-once dedup) + `ddl/clickhouse.sql` (ReplacingMergeTree fact + AggregatingMergeTree `quantileState` MV; `${OLAP_DB}`) + `ddl/bigquery.sql` (PARTITION+CLUSTER + materialized view; `${OLAP_DS}`) + `ddl/row-policy.template.sql` (defense-in-depth ClickHouse ROW POLICY + BigQuery row-access-policy/authorized view; `${ENV}`) + `tests/aggregation_behavior_test.py` (T1–T5 lineage/PII/maliyet/rollup/retention) + `run_live_test.sh` (statik + clickhouse-client/bq varsa canlı DDL; yoksa SKIP) + README. **İnvariant'lar:** A1 tenant_id ORDER BY/CLUSTER **ilk anahtar** (FR-TEN-002), A2 event_date partition, A3 sonlu TTL + katman monotonluğu (turn 90g < operational 730g < usage no-loss 2555g, FR-REC-006/010), A4 **her sütun pii_class∈{none,low,redacted}** ham PII/transkript/kart-OTP yasak (FR-REC-004/005), A5 residency home-region (NFR 10.7), A6 dedup_key idempotent (1.1.8 P3), A7 sorgu-katmanı predicate (istemciye güvenilmez) + row-policy, A8 no-loss retention≥365+idempotency_key, A9 rollup→fact+FR-ANA izi, A10 literal sır yok, A11 **spec↔DDL birebir** (ClickHouse+BigQuery), A12 lineage source_topic∈1.1.8 + idempotent consumer, A13 agent_version_id (FR-ANA-010), A14 ayrık STT/LLM/TTS/e2e gecikme (FR-ANA-006). validate **434/434** 🟢 + query **8/8** 🟢 (containment_rate=0.75 elle-hesap birebir, sızıntı=0) + selftest **15/15** 🟢 + behavior **12/12** 🟢; canlı motor yok → SKIP (exit 0). Kapsam: 1.1.8'de ertelenen **OLAP sink şeması** burada karşılandı; tiered/cold replay (1.1.8 P2) bu OLAP'a bağlanır. Retention motoru→1.2.3, residency zorlama→1.2.2 bu TTL/per-region sözleşmesini tüketir. Vendor-neutral (ADR-002); sır repoya yazılmadı. → `reports/1.1.9-olap-analitik-sema-raporu.md`

### 1.2 Veri yönetişimi
- [?] **1.2.1** Row-level security politikaları (her tablo `tenant_id`) — `F1` · `Must` · →FR-TEN-002
- [?] **1.2.2** Veri yerleşimi (residency): home-region eşlemesi + bölge zorlama — `F2` · `Must` · →NFR 10.7
- [?] **1.2.3** Retention motoru (otomatik saklama + geri döndürülemez silme) — `F1` · `Must` · →FR-REC-006/010
- [?] **1.2.4** Legal hold mekanizması — `F2` · `Should` · →FR-REC-007
- [?] **1.2.5** Backup/restore + RPO doğrulama (config ≤5dk, analitik ≤15dk) — `F2` · `Must` · →NFR 10.5

---

## 2. Telefoni & Gerçek Zamanlı Medya

### 2.1 Telephony edge / çağrı kontrolü
- [?] **2.1.1** SBC kurulumu (SIP güvenlik, topoloji gizleme, DDoS/SIP saldırı) — `F1` · `Must` · →NFR 10.6, FR-TEL-002
- [?] **2.1.2** SIP App Server (call setup/teardown, routing) — `F1` · `Must` · →FR-TEL-001
- [x] **2.1.3** Managed CPaaS entegrasyonu (Media Streams/WebSocket) — `F1` · `Must` · →FR-TEL-002 · ⇒ `telephony/managed-cpaas/` (SAD §7.2 **M1 managed-WS** modunun + API §11.5 `TelephonyAdapter` SPI managed modunun fiziksel sözleşmesi; `db/`+`cache/`+`objstore/`+`eventstream/` disipliniyle aynı: vendor-neutral ADR-002 / credential-free / statik probe + deterministik normalize simülatörü): `cpaas-spec.json` (kaynak doğruluk: media_profile 8kHz/20ms/μ-law/no-transcode + transport wss + medya-öncesi auth + context_binding + **normalize sözleşmesi** start/media/dtmf/stop ingress + play_audio/mark/barge_in_clear egress + **2 sağlayıcı** Twilio/Telnyx ingress/egress eşleme `${ENV}` secret/region + lifecycle 6-durum + barge_in 200ms + dtmf + reconnect + error_taxonomy→API §11.6 + residency + pii + **invariant I1–I14**) + `managed-cpaas.md` (tasarım: SPI bağlama, normalize kalbi, güvenlik/bağlam SAD §13.3, çerçeveleme, lifecycle/barge-in ADR-009/005, dayanıklılık, residency/PII, izlenebilirlik) + `cpaas_probe.py` (stdlib-only: `validate`/`normalize <sample>` deterministik medya-WS normalize sim. lifecycle SM + invariant kapısı/`selftest`/`schema`) + `config/providers.json` (Twilio primary + Telnyx secondary, endpoint/secret yalnız `${ENV}`, fallback sırası) + `samples/` (inbound-happy-path Twilio + barge-in Telnyx geçer · degraded bilinçli eler, FR-TST-008) + `tests/framing_behavior_test.py` (μ-law/8kHz/20ms aritmetiği + base64 round-trip T1–T5) + `run_live_test.sh` (statik + sample normalize; `${CPAAS_MEDIA_WS_URL}` varsa canlı not, yoksa SKIP). **validate 56/56** 🟢 + **selftest 15/15** 🟢 (negatif kapı kanıtı: plaintext WS/tek sağlayıcı/16kHz/barge-in 250ms/literal secret/taksonomi-dışı eler) + normalize 3 sample 🟢 (happy/barge-in geçer 8/8, degraded 🔴 3/7 beklenen I9+I4+I11+I3) + framing 5/5 🟢. **Bugfix/not:** sır tarayıcı `$comment`/`${ENV}` elenir (eventstream deseni); barge-in `barge_in_clear_overhead_ms` ile deterministik modellenir → 260ms overhead kapıyı elatır (ADR-009 hibrit managed tarafta da geçerli). Kapsam dışı (bilinçli): BYOC/M2→2.1.4, SBC→2.1.1, SIP App Server→2.1.2, CC/M3→F2, gerçek RTT/jitter→vendor-eval 0.2.1, RTP/codec→2.2.1/2.2.2. Vendor-neutral (ADR-002); sır repoya yazılmadı. → `reports/2.1.3-managed-cpaas-media-streams-raporu.md`
- [x] **2.1.4** SIP trunk / BYOC desteği — `F2` · `Must` · →FR-TEL-002 · ⇒ `telephony/byoc-sip/` (SAD §7.2 **M2 ham SIP trunk + RTP (BYOC)** modunun + API §11.5 `TelephonyAdapter` SPI BYOC modunun fiziksel sözleşmesi; 2.1.3 managed-cpaas (M1) disipliniyle birebir: vendor-neutral ADR-002 / credential-free / statik probe + deterministik SIP/SDP/RTP normalize simülatörü): `byoc-spec.json` (kaynak doğruluk: media_profile 8kHz/20ms/G.711 μ-law+A-law/no-transcode + transport SIPS/TLS sinyalleşme + SRTP medya + SBC-fronted + auth medya-öncesi digest/IP-ACL/mTLS + context_binding **DID→tenant** + **normalize sözleşmesi 2.1.3 ile birebir aynı** answer→start/rtp→media/dtmf→dtmf/bye→stop + **SIP diyalog SM** 9-durum [RTP-before-answer reddi, **183 early-media istisnası**] + **SDP** narrowband-only [ortak codec yoksa transcode değil **488 REDDET**] + **2 trunk** primary/secondary ingress/egress eşleme `${ENV}` secret/region + **fallback çapraz-mod M1** + barge_in 200ms `rtp_playout_stop` + dtmf RFC2833/SIP-INFO + transfer SIP-REFER + reconnect re-INVITE/CSeq + error_taxonomy SIP-kodu→API §11.6 + residency + pii + **invariant I1–I15**) + `byoc-sip.md` (tasarım: SPI bağlama, SIP/SDP/RTP→normalize kalbi, güvenlik/bağlam SAD §13.3, diyalog SM, barge-in ADR-009/005, dayanıklılık+çapraz-mod fallback, residency/PII, izlenebilirlik) + `byoc_probe.py` (stdlib-only: `validate` statik invariant + config↔spec çapraz + literal-sır / `normalize <sample>` deterministik SIP/SDP/RTP normalize sim. diyalog SM + SDP müzakere + invariant kapısı / `selftest` / `schema`) + `config/trunks.json` (primary[ip_acl/rfc2833] + secondary[digest/sip_info], SBC peer/secret/region yalnız `${ENV}`, fallback sırası + çapraz-mod M1) + `samples/` (inbound-happy-path primary + barge-in secondary [183 early-media+SIP-INFO+25ms clear] geçer · degraded bilinçli eler, FR-TST-008) + `tests/sdp_rtp_behavior_test.py` (SDP codec müzakere + G.711/RTP çerçeveleme aritmetiği T1–T5) + `run_live_test.sh` (statik + sample normalize; `${BYOC_SBC_HOST}` varsa canlı not, yoksa SKIP) + README. **validate 73/73** 🟢 + **selftest 22/22** 🟢 (negatif kapı kanıtı: düz-metin SIP/SRTP-yok/tek-trunk/16kHz/barge-in-250ms/literal-secret/taksonomi-dışı/wideband-SDP/transcode-on-no-common eler; normalize tarafında rtp-before-answer I9, **early-media RTP meşru geçer**, wideband-RTP I4/I15, eksik-bağlam I3, bölge-uyumsuz I11, 260ms-overhead I5, tcp-sinyalleşme I1, bilinmeyen-trunk + terminal-sonrası-geçiş kontrollü hata; secondary trunk SIP-INFO da geçer ADR-002 simetri) + normalize 3 sample 🟢 (happy/barge-in 9/9 geçer, degraded 🔴 2/9 beklenen I1×2+I9+I4/I15+I11+I3+I5) + sdp_rtp_behavior 9/9 🟢. Kapsam dışı (bilinçli): SBC kurulumu→2.1.1, SIP App Server→2.1.2, CC/M3→F2, E.164/numara havuzu→2.1.5, gerçek RTT/jitter→vendor-eval 0.2.1 M2, RTP/jitter buffer+codec→2.2.1/2.2.2, edge VAD→2.2.3. Vendor-neutral (ADR-002); sır repoya yazılmadı. → `reports/2.1.4-byoc-sip-trunk-raporu.md`
- [x] **2.1.5** E.164 normalizasyonu + Caller ID & numara havuzu yönetimi — `F1` · `Must` · →FR-TEL-004/005 · ⇒ `telephony/numbering/` (telefoni **numaralandırma düzlemi** — medya düzlemi DEĞİL; 2.1.3 managed-cpaas / 2.1.4 byoc-sip adaptörlerini **besler**: inbound DID→tenant(+agent) çözümü → `context_binding.did_to_tenant_map` SAD §13.3, outbound Caller ID seçimi → API §11.5 `DialRequest.callerIdPool/from`; vendor-neutral ADR-002 / credential-free / stdlib-only, önceki dilim disipliniyle birebir): `numbering-spec.json` (kaynak doğruluk: **E.164 sözleşmesi** kanonik `+`+7..15 hane/idempotent/geçersiz-reddi + **ülke metadata** TR/GB/DE/US [DPIA residency profilleriyle hizalı, libphonenumber DEĞİL — minimal deterministik çekirdek, tam metadata canlı adaptörde] + **numara havuzu** tenant-scoped ama **e164 GLOBAL benzersiz** DB §14 UNIQUE/FR-TEN-002 + yön[inbound/outbound/both] + bölge pini NFR 10.7 + inbound `agent_id` + **Caller ID politikası** anti-spoof[yalnız havuz+outbound-yetkili numara, FR-AUTH-001 ruhu/TM-S] + seçim[requested→campaign-pin→local-presence→e164-sıralı varsayılan] + CLI sunumu[withhold yalnız `cp.outbound.cli_presentation` DPIA] + error_taxonomy→API §11.6 + residency + pii[müşteri/callee numarası SAKLANMAZ; havuz illüstratif ayrılmış aralıklar] + **invariant N1–N11**) + `numbering.md` (tasarım: mimari konum/akış, normalize çekirdeği [IDD `00`/NANP `011` vs trunk `0`/NANP `1` ayrımı, NANP `1`=trunk∧cc çakışması NSN ile çözülür], anti-spoof, residency/PII, kapsam ayrımı) + `numbering_probe.py` (stdlib-only: `validate` spec+config invariant kapısı / `normalize <sample>` deterministik E.164 normalize sim. çeşitli biçim→kanonik+idempotency+reddi / `select <sample>` deterministik Caller ID seçim sim. anti-spoof+local-presence+residency+outbound-yetki / `resolve <e164>` inbound DID→tenant+agent çapraz-tenant benzersizlik / `selftest` / `schema`) + `config/number-pool.json` (2 tenant illüstratif havuz; NANP 555-01XX + Ofcom 01632 960xxx/07700 900xxx + TR 0850 ayrılmış aralıklar; sır yok) + `samples/` (normalize-valid 13-case [9 normalize+4 reddi] · caller-id-local-presence [UK callee→+441632960123] · caller-id-campaign [camp-winback→+447700900123] geçer · caller-id-spoof [havuz-dışı talep] anti-spoof reddi · normalize-degraded bilinçli eler, FR-TST-008) + `tests/e164_behavior_test.py` (T1–T6: regex sınırı/biçim temizliği/IDD-trunk ayrımı/NANP çakışması/NSN uzunluk reddi/Caller ID anti-spoof) + `run_live_test.sh` (statik + sample; `${NUMBERING_API_URL}` varsa canlı not, yoksa SKIP) + README. **validate 45/45** 🟢 + **selftest 32/32** 🟢 (negatif kanıt: çapraz-tenant e164 çakışma N4, havuz-dışı/inbound-only caller id talebi N6, compliance'sız CLI gizleme N9, bölge uyumsuz N7, >15-hane/kısa-NSN/hanesiz girdi reddi N3, taksonomi-dışı N10, literal secret N11) + **e164_behavior 12/12** 🟢 + 5 sample beklendiği gibi + resolve inbound DID→tenant 🟢. Kapsam dışı (bilinçli): medya/SIP/RTP→2.1.3/2.1.4, SBC/SIP App Server→2.1.1/2.1.2, DTMF/neden kodları→2.1.6/2.1.7, outbound consent/saat/DNC **zorlaması**→FR-TEL-013/014 (burada yalnız Caller ID kaynağı+compliance bağlama), `phone_number` fiziksel migration→DB §14, tam dünya E.164 metadata→canlı adaptör. Vendor-neutral (ADR-002); sır repoya yazılmadı. → `reports/2.1.5-e164-caller-id-numara-havuzu-raporu.md`
- [x] **2.1.6** DTMF algılama/üretme (RFC 2833 / SIP INFO) — `F1` · `Must` · →FR-TEL-006 · ⇒ `telephony/dtmf/` (telefoni **DTMF olay düzlemi** — TelephonyAdapter içi; 2.1.3 managed-cpaas (M1) / 2.1.4 byoc-sip (M2) yüzey-seviye DTMF'ini birinci-sınıf deterministik sözleşmeye çevirir; 2.1.5 disipliniyle birebir: vendor-neutral ADR-002 / credential-free / stdlib-only / gerçek kart-PIN-RTP yok): `dtmf-spec.json` (kaynak doğruluk: **yalnız out-of-band** — RFC 2833/4733 RTP telephone-event + SIP INFO (RFC 2976), in-band KAPALI [no-transcode 8kHz hat FR-RES-008] + **olay kodu→basamak eşlemesi** 0..15→0-9*#A-D, >15 reddi + **algılama** debounce[çok-paketli marker+ara+End-bit artık→tek mantıksal basamak, kayıp-End dayanıklılığı]+süre kapısı[<min_duration_ms sahte düşürülür]+SIP INFO[application/dtmf-relay Signal=/Duration= + application/dtmf]→birleşik normalize `dtmf` olayı [mode-agnostik, API §12.1/§12.2, SAD §6.1] + **üretme** sendDtmf[API §11.5; müzakere modu, basamak başına paket/INFO dizisi + inter-digit boşluk, gönderim öncesi doğrulama] + **PCI maskeleme** [BRD §8.5/SEC-17/TM-I-03: hassas pencere → basamak yalnız aksiyon kanalı, transkript/kayıt/gözlemlenebilirlik MASK_TOKEN, sızmaz, FR-REC-004/005] + **barge-in** [DTMF turn girdisi → TTS ≤200ms kesme ADR-005/009] + error_taxonomy→API §11.6 + residency NFR 10.7 + **invariant D1–D12**) + `dtmf.md` (tasarım: mimari konum, out-of-band ilkesi, RFC 2833 debounce, SIP INFO, PCI maskeleme, turn entegrasyonu, izlenebilirlik) + `dtmf_probe.py` (stdlib-only: `validate` statik invariant+config+sır / `detect <sample>` deterministik RFC 2833 debounce+SIP INFO+süre kapısı+maskeleme sim. / `generate <sample>` deterministik sendDtmf paket/INFO dizisi sim. / `selftest` / `schema`) + `config/dtmf-modes.json` (3 illüstratif profil: M1 rfc2833, M2 primary rfc2833, M2 secondary sip_info; sır yok) + `samples/` (detect-rfc2833-happy `15*` debounce · detect-sip-info `9#37` · detect-pci-masked `1234`→maskeli geçer · generate-rfc2833 `1809#` · generate-sip-info `42A` geçer · detect-degraded[kod 16 aralık-dışı] + generate-degraded[basamak X] bilinçli eler, FR-TST-008) + `tests/dtmf_behavior_test.py` (T1–T6: debounce/süre-kapısı-aritmetiği/kod-sınırı/SIP-INFO/üretme/maskeleme-sızıntı) + `run_live_test.sh` (statik + sample; `${DTMF_API_URL}` varsa canlı not, yoksa SKIP) + README. **validate 42/42** 🟢 + **selftest 38/38** 🟢 (negatif kanıt: in-band-açık D1, bozuk-kod-eşlemesi D3, maskeleme-bozuk D8, taksonomi-dışı D10, min_duration=0 D4, kart-verisi-yasak-kapalı D12, literal-secret D12 → validate eler; kod 16 + basamak X detect/generate reddi; round-trip üret→algıla `159*` birebir) + **dtmf_behavior 30/30** 🟢 + 6 sample beklendiği gibi (4 pass + 2 fail). Kapsam dışı (bilinçli): tam PCI ödeme akışı→17.2.6 (F3), SBC/SIP App Server→2.1.1/2.1.2, medya/RTP jitter buffer→2.2.x, canlı RFC2833 RTP+SIP INFO→F1 gerçek trunk. Vendor-neutral (ADR-002); sır/kart verisi repoya yazılmadı. → `reports/2.1.6-dtmf-detection-generation-raporu.md`
- [x] **2.1.7** Çağrı başlangıç/bitiş neden kodları (standart taksonomi) — `F1` · `Must` · →FR-TEL-012 · ⇒ `telephony/reason-codes/` (çağrı yaşam-döngüsü **neden-kodu düzlemi** — medya/numaralandırma DEĞİL; sağlayıcıdan bağımsız kanonik neden taksonomisi + sinyal→kod sınıflama sözleşmesi, FR-TEL-012; 2.1.5/2.1.6 disipliniyle birebir: vendor-neutral ADR-002 / credential-free / stdlib-only): `reason-codes-spec.json` (kaynak doğruluk: **kapalı enum kümeleri** [category/terminal_status=call.status API §6/outcome FR-ANA-002-003/party/event_type/direction] + **5 start_reasons** [inbound_pstn/inbound_did_routed/outbound_campaign/outbound_api/outbound_callback] + **~25 end_reasons** tam nitelik kümeli [6 kategori: normal·no_contact·failed·abandoned·compliance·system; her kod category/terminal_status/outcome/party/billable/retryable/event_type/error_taxonomy] + **signal_map** 4 kaynak [sip yanıt kodu/q850 cause/cpaas durum dizesi/internal orkestratör olayı]→kanonik kod, eşlenmeyen→`unknown` fallback+flag [sessiz yutma yok] + residency region-pin + pii [neden kodu PII içermez, pii_class=none, OLAP A4] + **invariant R1–R13**) + `reason-codes.md` (tasarım: mimari konum, taksonomi, sinyal eşleme, gözlemlenebilirlik/analitik bağlama OLAP 1.1.9 fct_call.end_reason + FR-ANA-002 containment + FR-TEL-015 silent-call oranı, kapsam ayrımı) + `reason_codes_probe.py` (stdlib-only: `validate` R1–R13+config / `classify <sample>` deterministik sinyal→kanonik kod+tüm nitelikler sim. [expect doğrulama + fallback→kapı eler R4] / `lookup <code>` / `selftest` / `schema`) + `config/provider-reason-map.json` (≥2 sağlayıcı Twilio/Telnyx M1 + ham SIP M2 örtüşmesi, hedefler kanonik koda çözülür, sır yok) + `samples/` (classify-normal-hangup/busy-sip/transfer/dnc-blocked/voicemail-left/abandoned-capacity geçer · degraded[eşlenmemiş SIP 699→unknown] bilinçli eler, FR-TST-008) + `tests/reason_codes_behavior_test.py` (T1–T6: kategori kapanışı/status↔event/retryable disiplini/compliance/sinyal çözümü/fallback) + `run_live_test.sh` (statik+sample+behavior; `${REASON_CODES_API_URL}` varsa canlı not, yoksa SKIP) + README. **validate 294/294** 🟢 + **selftest 28/28** 🟢 (negatif kapı kanıtı: compliance-billable R5/normal-retryable R8/signal_map-orphan R4/fallback-silme R4/taksonomi-dışı-error R7/status↔event-uyumsuz R11/PII-koruması-kapalı R13/voicemail-billable R9/geçersiz-kod-adı R12 → validate eler) + **behavior T1–T6 🟢** + 6 sample beklendiği gibi (5 pass + degraded R4 eler). Kapsam dışı (bilinçli): retry zamanlaması→2.1.8 (burada yalnız `retryable` bayrağı + `outbound_callback`), AMD/voicemail akışı→2.1.9 (yalnız `voicemail_*` kodları), consent/DNC/saat **zorlaması**→FR-TEL-013/014 (yalnız `blocked_*` kodları), faturalandırma motoru→FR-BIL (yalnız `billable` bayrağı), medya/SIP/RTP→2.1.3/2.1.4, DTMF→2.1.6, canlı event yayını→F1+0.4.7. Vendor-neutral (ADR-002); PII/sır repoya yazılmadı. → `reports/2.1.7-cagri-neden-kodlari-taksonomi-raporu.md`
- [x] **2.1.8** Çağrı düşmesinde kontrollü retry/geri arama — `F2` · `Should` · →FR-TEL-009 · ⇒ `telephony/retry-callback/` (çağrı yaşam-döngüsü **"sonra ne olacak" karar düzlemi** — medya/numaralandırma/taksonomi DEĞİL; 2.1.7 reason-codes `retryable` bayrağını + `outbound_callback` start kodunu **tüketir**; 2.1.5/2.1.6/2.1.7 disipliniyle birebir: vendor-neutral ADR-002 / credential-free / stdlib-only / **deterministik** [sanal saat + tohumlu jitter]): `retry-callback-spec.json` (kaynak doğruluk: **policy_model** [max_attempts FR-OUT-005 sert tavan + fixed/exponential backoff sınırlı + jitter_cap + calling_hours + mode + drop_grace] + **decision_contract** request/response + **eligibility.classes** [dropped=`dropped_mid_call` FR-TEL-009 çekirdek / no_contact=cevapsız·meşgul·AMD·teknik / capacity=abandoned·blocked — 2.1.7 retryable kodlarına eşlenir, boşluk=kapı eler] + **compliance_gates** [G1 DNC FR-TEL-014/FR-OUT-006 → suppress · G2 consent FR-OUT-003 → suppress · G3 non-retryable 2.1.7 R8 → no_action · G4 max FR-OUT-005 → suppress · G5 kapasite FR-RES-014/FR-OUT-007 → callback; **zamanlamadan önce** compliance-by-design] + residency region-pin + pii [karar PII içermez, contact_id referansı] + **invariant C1–C14**) + `retry-callback.md` (tasarım: karar akışı, uygunluk sınıfları, backoff/jitter/sınır kontrol garantileri, gözlemlenebilirlik geri-bildirim döngüsü, kapsam ayrımı) + `retry_callback_probe.py` (stdlib-only: `validate` C1–C14 + 2.1.7 çapraz-tutarlılık + config / `decide <sample>` deterministik tek-karar sim. [uyum kapıları → uygunluk sınıfı → backoff+jitter → arama-saati pencere; allow_error/expect] / `simulate <sample>` çoklu-deneme tükenme [C2 tavan asla aşılmaz kanıtı] / `selftest` / `schema`) + `config/retry-policies.json` (3 politika: campaign-default exponential 3-deneme 09-18 + transactional-callback fixed callback-modu + high-priority-reconnect agresif-ama-sınırlı; sır yok) + `samples/` (decide-drop-retry [drop_grace 30s] · no-answer-backoff [exp 1200s] · max-attempts [tavan→suppress] · dnc-suppress · consent-withdrawn · outside-hours [ileri-sar] · capacity-callback · non-retryable [no_action] geçer · degraded [C12 retryable uyuşmazlığı] bilinçli eler + simulate-exhaustion; FR-TST-008) + `tests/retry_callback_behavior_test.py` (T1 sınır/T2 backoff+jitter/T3 uyum kapıları/T4 arama saati/T5 kapasite/T6 idempotency) + `run_live_test.sh` + README. **validate 44/44** 🟢 + **selftest 28/28** 🟢 (negatif kapı kanıtı: G1 DNC=retry C5 / start_reason≠outbound_callback C9 / PII-koruması-kapalı C11 / dropped-sınıfı-silindi C13 → validate eler; retryable taksonomi-uyuşmazlığı + taksonomi-dışı kod → decide hata) + **9 decide sample** beklendiği gibi (8 pass + degraded C12 eler) + **simulate** 🟢 (no_answer her tur düşer → 3 denemede max_attempts'te durur, sonsuz retry yok) + **behavior T1–T6** 🟢. **Bulgu/kontrol garantisi:** "kontrollü" = sert tavan (C2) + sınırlı monotonik backoff (C3) + deterministik jitter (C4, retry fırtınası önleme SR-TOOL-003) + uyum-önce (C5/C6, DNC/consent zamanlamadan önce) + idempotent (C10, çift zamanlama yok); `outbound_callback` ile başlayan çağrı 2.1.7'ye geri beslenir → kapalı döngü. Kapsam dışı (bilinçli): scheduler/dialer altyapısı→F1+3.x, AMD/voicemail→2.1.9, consent/DNC/saat kayıt&zorlama kaynağı→DB §5.4+FR-TEL-013/014, backpressure mekanizması→FR-RES-014+0.4.8, taksonomi→2.1.7. Vendor-neutral (ADR-002); PII/sır repoya yazılmadı. → `reports/2.1.8-kontrollu-retry-geri-arama-raporu.md`
- [x] **2.1.9** Answering machine detection + voicemail bırakma — `F2` · `Must` · →FR-TEL-010/011 · ⇒ `telephony/amd-voicemail/` (çağrı **algılama+aksiyon düzlemi** — "kim/ne cevapladı + ne yapmalı"; medya kodek/RTP DEĞİL → 2.2.x; 2.1.7 reason-codes `voicemail_machine_detected`/`voicemail_left` kanonik kodlarını **ÜRETİR** → 2.1.8 retry tüketir; 2.1.5–2.1.8 disipliniyle birebir: vendor-neutral ADR-002 / credential-free / stdlib-only / **deterministik** [sanal saat; random YOK]): `amd-voicemail-spec.json` (kaynak doğruluk: enums [amd_class human/machine/unknown · amd_decision · voicemail_outcome · signal_source] + **detection_contract** [signals: CPaaS AMD ipucu 2.1.3 ∨ medya heuristiği greeting/beep/silence] + **drop_contract** + **amd_model** [feature_weights beep0.45/greeting0.35/silence0.20 **toplam 1.0**→p_machine∈[0,1] + sıralı eşikler + `unknown_fallback=treat_as_human` insan-güvenli] + **voicemail_model** [enabled/beep_required/max_beep_wait_ms/max_message_seconds/require_disclosure] + **compliance_gates** [G1 policy-off→not_left_policy_off · G2 consent FR-OUT-003 · G3 DNC FR-TEL-014 · G4 AI-ifşası BRD §14.2 · G5 bip A4 → bırakmadan ÖNCE, her engel açık outcome] + **reason_code_map** [machine_not_left→voicemail_machine_detected retryable→2.1.8 · left→voicemail_left billable; 2.1.7 çapraz-doğrulanır A7] + **accuracy_gate** [SR-TEL-010 T: min_accuracy≥0.90 + **max_false_machine_rate≤0.05** kritik insan→makine güvenlik metriği] + residency/pii [karar PII içermez; voicemail `message_ref` TTS-şablon/kayıt-id, ham audio değil A11] + **invariant A1–A14**) + `amd-voicemail.md` (tasarım: sınıflandırıcı iki-kaynak, insan-güvenli A2/A3, doğruluk kapısı T-yöntem, bırakma G1–G5 bip-sonrası A4, 2.1.7 köprüsü, kapsam ayrımı) + `amd_voicemail_probe.py` (stdlib-only: `validate` A1–A14+config+2.1.7 çapraz / `classify <s>` deterministik AMD [CPaaS ipucu öncelik→heuristik p_machine→eşik; expect] / `drop <s>` voicemail dizisi [uyum/bip kapıları→outcome+reason_code; expect] / `accuracy <ds>` etiketli küme doğruluk+false_machine_rate→SR-TEL-010 kapı / `selftest` / `schema`) + `config/amd-profiles.json` (3 profil: campaign-voicemail [drop+bip+ifşa, min_conf 0.80] · detect-only [enabled=false → yalnız algıla+2.1.8] · high-confidence-voicemail [min_conf 0.90]; sır yok) + `samples/` (classify-human/machine-cpaas/machine-heuristic/unknown · drop-voicemail-left[SR-TEL-011 D]/no-beep/policy-off/consent geçer · drop-degraded[insan→A12 hata] + accuracy-labeled-set[geçer 0.917]/accuracy-degraded[false_machine 1.0 eler] bilinçli, FR-TST-008) + `tests/amd_voicemail_behavior_test.py` (T1 sınıflandırma/T2 belirsiz A3/T3 kaynak önceliği/T4 bırakma A4/A7/T5 uyum kapıları A5/A6/T6 determinizm+bip sınırı) + `run_live_test.sh` + README. **validate 47/47** 🟢 + **selftest 26/26** 🟢 (negatif kapı kanıtı: ağırlık toplamı≠1.0 A2 / unknown_fallback insan-güvenli-değil A3 / voicemail_left yanlış eşleme A7 / ham-audio koruması kapalı A11 / G2 consent outcome=left A6 → validate eler; tek-bip 0.45<0.80 tek başına machine değil = aşırı-iddia yok; insan classify→drop hata A12) + **4 classify + 4 drop sample** beklendiği gibi (+ drop-degraded A12 eler) + **accuracy** 🟢 (labeled 0.917≥0.90 + false_machine 0.000≤0.05 geçer; degraded false_machine 1.0 + doğruluk 0.50 eler) + **behavior T1–T6** 🟢. **Bulgu/güvenlik tasarımı:** false_machine_rate (insanı makine sanmak) **kritik metrik** — sıkı sınırlanır; `unknown→treat_as_human` belirsizde insanı korur (voicemail bırakmaz/sessiz kalmaz); bip heuristikte güçlü işaret olduğundan bipsiz makine tespiti CPaaS ipucuna dayanır (greeting analizi bip-öncesi makine der). Kapsam dışı (bilinçli): AMD DSP/sinyal işleme→F1 edge ADR-009, voicemail medya egress/TTS→0.2.3+2.2.x, disposition FR-OUT-008→10.1.4/10.1.5, consent/DNC kayıt&zorlama→DB.md §5.4+FR-TEL-013/014, retry zamanlaması→2.1.8, taksonomi→2.1.7, numara→2.1.5, medya/SIP/RTP→2.1.3/2.1.4, canlı AMD doğruluğu+bırakma→F1 gerçek CPaaS AMD+medya egress+örneklemli QA. Vendor-neutral (ADR-002); numara/transkript/ham-audio/sır repoya yazılmadı. → `reports/2.1.9-amd-voicemail-raporu.md`

### 2.2 Real-time media gateway
- [x] **2.2.1** RTP/medya sonlandırma + jitter buffer — `F1` · `Must` · →FR-RTC-001 · ⇒ `telephony/rtp-jitter/` (Real-time **Media Gateway** ingress medya yolu — SAD §6/§7, L2; 2.1.3 managed-cpaas (M1) / 2.1.4 byoc-sip (M2) taşıma/normalize sözleşmesini kurdu, bu dilim taşıma içindeki gerçek **RTP sonlandırma + adaptif jitter buffer** çekirdeğini birinci-sınıf deterministik sözleşmeye çevirir; 2.1.x disipliniyle birebir: vendor-neutral ADR-002 / credential-free / stdlib-only / **deterministik** [sanal saat; random YOK]): `rtp-jitter-spec.json` (kaynak doğruluk: media_profile 8kHz/20ms/50fps/160-örnek no-transcode FR-RES-008 + **rtp_termination** [16-bit seq + 32-bit ts wraparound-güvenli + SSRC-değişimi-reset + marker-reprime + pencere-içi-reorder, RFC 3550] + **jitter_buffer** [adaptif: target=clamp(base+factor·J, min, max); fixed mod; late→discard; overflow→drop_oldest FR-RES-016/014; concealment→plc_fill] + **gates** [J3 eklenen playout gecikme P95 ≤80ms SAD §20 medya bütçesi payı/green ≤50ms + J5 concealment ≤0.05 aşılırsa degrade FR-RTC-005 + J6 sürekli kadans stream-first + J1 reorder-absorbe] + **metrics** [jitter/loss/concealment/eklenen-gecikme → BRD §15 → observability 0.4.7 voice_jitter_ms/voice_packet_loss_ratio; call_id metrik label OLMAZ] + barge_in_flush ≤200ms kancası [algılama 2.2.7/edge ADR-005] + error_taxonomy→API §11.6 + residency NFR 10.7 + pii [ham RTP payload/transkript YOK] + **invariant J1–J14**) + `rtp-jitter.md` (tasarım: mimari konum, RFC 3550 sonlandırma, adaptif derinlik, kayıp concealment + sürekli kadans, **loss↔latency gerilimi**, metrikler, kapsam ayrımı) + `rtp_jitter_probe.py` (stdlib-only: `validate` J1–J14+config+sır / `simulate <sample>` **deterministik jitter buffer simülatörü** [wrap-güvenli RTP aritmetiği + RFC 3550 interarrival jitter + adaptif playout + reorder/late/loss muhasebesi + eklenen-gecikme P95 lineer-interp percentile + HARD kapı→çıkış kodu] / `selftest` / `schema`) + `config/jitter-profiles.json` (3 profil: managed-ws-default [base 30 adaptif] · byoc-rtp-default [base 40 adaptif daha-derin] · fixed-conservative [sabit 30 referans]; bölge pini `${ENV}`; sır yok) + `samples/` (jitter-happy-path/adaptive-depth/reorder-absorbed/loss-concealed[%4 ≤%5 PLC] geçer · jitter-degraded[%25 kayıp + aşırı jitter → J5+J3 eler] bilinçli, FR-TST-008) + `tests/jitter_behavior_test.py` (T1 wrap-aritmetik/T2 RFC3550-jitter/T3 adaptif-derinlik-clamp/T4 reorder-absorbe/T5 kayıp-concealment/T6 geç-atma+kadans) + `run_live_test.sh` (statik + sample; `${MEDIA_GW_URL}` varsa canlı not, yoksa SKIP) + README. **validate 52/52** 🟢 + **selftest 41/41** 🟢 (negatif kapı kanıtı: no_transcode-kapalı J8 / varsayılan-adaptive-değil J2 / min>max-derinlik J2 / green>bütçe J3 / concealment-aralık-dışı J5 / taksonomi-dışı J12 / geç-politika-discard-değil J4 / ham-payload-yasak-kapalı J14 / literal-secret J14 → validate eler; arrival_ms'siz + hizasız-ts + desteklenmeyen-mod + boş-akış → simulate reddi) + **jitter_behavior 20/20** 🟢 + 5 sample beklendiği gibi (4 pass + degraded eler). **Bulgu/gerilim:** eklenen gecikme P95 ≈ nominal buffer derinliği → buffer'ın J2'de izinli 120ms'e büyümesi loss'u önler ama J3 latency bütçesini (80ms) aşar (**loss↔latency gerilimi**); degraded sample derinliği 120ms'e clamp'ler (kayıp kaçınma) + concealment %23 → hem J3 hem J5 eler = degrade sinyali → bölgesel düşük-jitter ağ + edge medya (ADR-009 hibrit) gereğini doğrular. Kapsam dışı (bilinçli): codec/8kHz→2.2.2, edge VAD/endpointing→2.2.3, echo→2.2.4, streaming STT connector→2.2.5, streaming TTS/ölü-hava→2.2.6, barge-in üretimi+TTS kesme→2.2.7 (yalnız flush kancası), SBC/SIP App Server→2.1.1/2.1.2, taşıma/normalize→2.1.3/2.1.4, gerçek RTT/jitter ölçümü→vendor-eval 0.2.1, native medya yığını (C/C++/Rust WebRTC)→SAD §21+F1 kod. Vendor-neutral (ADR-002); ham RTP payload/transkript/sır repoya yazılmadı. İz: FR-RTC-001 → SR-RTC-001 → TC-RTC-001. → `reports/2.2.1-rtp-media-termination-jitter-buffer-raporu.md`
- [x] **2.2.2** Codec yönetimi (8kHz), gereksiz resample/transcode önleme — `F1` · `Must` · →FR-RES-008 · ⇒ `telephony/codec/` (Real-time **Media Gateway** medya yolu codec seçimi + zincir format yönetimi — SAD §6/§7, L2; 2.1.3/2.1.4 taşıma + 2.2.1 RTP sonlandırma (codec-agnostik) üstüne **FR-RES-008**'i [8kHz zincirinde gereksiz resampling/transcoding önleme] ölçülebilir kapıya indirir; vendor-neutral ADR-002 / credential-free / stdlib-only / deterministik [random YOK]): `codec-spec.json` (kaynak doğruluk: media_profile 8kHz/20ms/50fps/160-örnek + **codec_catalog** [PCMU/PCMA/OPUS_NB 8kHz dar bant + OPUS_WB/L16_8K/L16_16K; encoding+rate_hz+payload_type, RFC 3551/7587/G.711] + **negotiation** [SDP offer/answer benzeri narrowband-first passthrough, ortak-yok→UNAVAILABLE] + **chain_analysis** [sınırda rate-farkı→resample, encoding-farkı→transcode; minimal-DP uçlar-pinned/iç-native; stage codec native zorunlu] + **gates** [C1 wire 8kHz dar bant + C2 pazarlık-optimal + C3 gereksiz-transcode=0 + C4 gereksiz-resample=0 + C5 ≤1 kontrollü resample noktası SAD §7.2 + C6 rate/encoding U-dönüşü yasak] + **metrics** [active_codec→voice_codec_info 0.4.7 codec-label; dönüşüm sayaçları] + error_taxonomy→API §11.6 + residency NFR 10.7/ADR-009 + pii [ham payload/transkript YOK] + **invariant C1–C14**) + `codec.md` (tasarım: katalog, pazarlık, zincir analizi + **gereksiz=actual−minimal-DP** tanımı, tek kontrollü resample seam, U-dönüşü, 8kHz native, kapsam ayrımı) + `codec_probe.py` (stdlib-only `validate`/`simulate <sample>`/`selftest`/`schema`; deterministik **pazarlık** [offer∩answer narrowband-first] + **zincir analizi** [resample/transcode hop sayımı + minimal-DP alt sınır + gereksiz dönüşüm + rate/encoding U-dönüşü] + HARD kapı→çıkış kodu) + `config/codec-profiles.json` (3 profil: managed-ws-g711 · byoc-rtp-g711-opus [uzak uç 16kHz Opus sunsa bile 8kHz seçer] · byoc-opus-nb; offer/answer; bölge pini `${ENV}`; sır yok) + `samples/` (codec-passthrough-g711 [0 dönüşüm] · codec-opus-nb-transcode [1 gerekli transcode] · codec-single-resample-stt16k [tek kontrollü 8k→16k C5] geçer · codec-redundant-transcode [µ→A→µ → C3+C6 eler] · codec-double-resample [8k→16k→8k→16k → C4+C5+C6 eler] bilinçli fail, FR-TST-008) + `tests/codec_behavior_test.py` (T1 pazarlık-narrowband-first/T2 reddetmeler/T3 minimal-DP/T4 passthrough+tek-resample/T5 gereksiz-transcode/T6 çift-resample+native-tutarlılık) + `run_live_test.sh` (statik + sample; `${MEDIA_GW_URL}` varsa canlı not, yoksa SKIP) + README. **validate 46/46** 🟢 + **selftest 34/34** 🟢 (negatif kapı: no_unnecessary_transcode-kapalı / resample_points_max≠1 / gereksiz-transcode-max≠0 / wire-16kHz / narrowband-first-kapalı / taksonomi-dışı / telefoni-8kHz-codec-yok / ham-payload-yasak-kapalı / literal-secret → validate eler; tanınmayan/ortak-yok codec + non-native-stage + tek-stage-zincir → pazarlık/analiz reddi) + **codec_behavior 20/20** 🟢 + 5 sample beklendiği gibi (3 pass + 2 fail). **Bulgu:** gereksiz dönüşüm iki katmanda önlenir — (1) pazarlıkta narrowband-first ile ortak 8kHz codec (kaynakta önleme) + (2) zincirde minimal-DP'ye göre fazla hop yakalama; ideal passthrough (G.711/Opus-NB) 0 dönüşüm, STT/TTS 16kHz native değilse tek kontrollü resample seam (SAD §7.2 birebir); çoklu-resample/encoding-U-dönüşü antipattern'leri C4/C5/C6 ile yakalanır (→ tek merkezi seam/ADR-009 hibrit gereğini doğrular). Vendor-neutral (ADR-002); ham ses payload/transkript/sır repoya yazılmadı. İz: FR-RES-008 → SR-RES-008 (A) → TC-RES-008 (RTM'de zaten eşli; SRS/RTM değişikliği gerekmedi). → `reports/2.2.2-codec-management-8khz-raporu.md`
- [x] **2.2.3** Edge VAD / endpointing (dinamik) — `F1` · `Must` · →FR-RTC-004/013, ADR-005 · ⇒ `telephony/edge-vad/` (Real-time **Media Gateway** **edge** katmanı — SAD §6/§7, L2; 2.2.1 jitter buffer'ın verdiği düzgün 20ms/50fps kadansı tüketir; VAD/endpointing+barge-in **edge'de** ADR-005 Kabul / ADR-009 hibrit; 2.2.1/2.2.2 disipliniyle birebir: vendor-neutral ADR-002 / credential-free / stdlib-only / **deterministik** [sanal saat; random YOK]): `edge-vad-spec.json` (kaynak doğruluk: media_profile 8kHz/20ms/50fps/160-örnek no-transcode FR-RES-008 + **edge_placement** at_edge=true media-gateway-L2 FR-RTC-013 + **vad** [adaptif noise floor + onset/offset histerezis + hangover mikro-duraklama köprüsü + barge-in echo_guard] + **endpointer** [DİNAMİK T_confirm=clamp(base + kısa-söz?+hesitation_extra + tekrar?+hesitation_extra − uzun-akıcı?completeness_discount, min, max) FR-RTC-004] + **barge_in** [min_speech_frames debounce + echo_guard, detection ≤200ms ADR-005/NFR 10.1] + **dead_air** [suppress_silence + finalize_only_at_endpoint FR-RES-009/FR-RTC-013] + **gates** [V5 söz-sonu gecikme P95 ≤250ms SAD §20 endpointing bandı/green ≤200 + V6 erken-kesme ≤0.05 hesitation köprüleme + V7 kaçırma ≤0.0 + V8 barge-in algılama P95 ≤200ms/green ≤100 + V9 yanlış-barge-in ≤0.0 + V10 ölü-hava-bastırma] + **metrics** [silence/barge-in → BRD §15 → observability 0.4.7 silence_duration_ms/barge_in_total; call_id metrik label OLMAZ] + error_taxonomy→API §11.6 + residency NFR 10.7 + pii [ham ses payload/transkript YOK, yalnız enerji metadata] + **invariant V1–V14**) + `edge-vad.md` (tasarım: edge konum gerekçesi [0.3.4 merkez barge-in P95≈266ms eler], VAD, dinamik onay-sessizliği, ground-truth puanlama, barge-in echo guard, dead-air kaynak kazancı, **iki gerilim** erken↔geç kesme + echo↔barge-in) + `edge_vad_probe.py` (stdlib-only: `validate` V1–V14+config+sır / `simulate <sample>` **deterministik VAD+dinamik endpointer+barge-in simülatörü** [adaptif eşik/histerezis/hangover + T_confirm bağlam-dinamiği + ground-truth karşı erken-kesme/kaçırma/gecikme puanlama + barge-in debounce/echo-guard + dead-air muhasebe + lineer-interp P95 percentile 0.3.x/2.2.1 ile birebir + HARD kapı→çıkış kodu] / `selftest` / `schema`) + `config/vad-profiles.json` (3 profil: managed-ws-default [temiz, base 160] · byoc-rtp-default [gürültülü, base 180, +hangover] · low-latency-quiet [çevik, base 140]; bölge pini `${ENV}`; sır yok) + `samples/` (happy-path[2 söz]/hesitation-held[240ms söz-içi duraklama köprülenir]/completeness-fast[uzun tur green]/barge-in[≤200ms]/barge-in-echo-guard[yanlış tetik yok]/dead-air-suppressed[%79 bastırma] geçer · edge-vad-degraded[aşırı gürültü → V7+V10 eler] bilinçli, FR-TST-008) + `tests/edge_vad_behavior_test.py` (T1 VAD-sınıflama/T2 hangover-köprü/T3 dinamik-T/T4 hesitation/T5 söz-sonu-gecikme/T6 barge-in+echo-guard/T7 dead-air) + `run_live_test.sh` (statik + sample; `${MEDIA_GW_URL}` varsa canlı not, yoksa SKIP) + README. **validate 56/56** 🟢 + **selftest 40/40** 🟢 (negatif kapı kanıtı: no_transcode-kapalı V12 / edge-kapalı V1 / dinamik-değil V2 / min>base V2 / green>bütçe V5 / endpoint-bütçe-SAD-bandı-aşımı V5 / barge-in->200ms V8 / sessizlik-bastırma-kapalı V10 / taksonomi-dışı V14 / ham-payload-yasak-kapalı V14 / literal-secret V14 → validate eler; sayısal-olmayan-enerji + boş-akış → simulate reddi) + **edge_vad_behavior 17/17** 🟢 + 7 sample beklendiği gibi (6 pass + degraded eler). **Bulgu/gerilim:** (1) statik onay-sessizliği erken↔geç kesme gerilimini çözemez → **dinamik T_confirm** kısa/kesik sözde uzayıp tereddüt duraklamasını köprüler (V6), uzun akıcı turda kısalıp gecikmeyi green'e çeker (V5); (2) **bugfix** — echo enerjisi noise floor'u yukarı çekip guard'lı eşiği yükselterek gerçek barge-in'i kaçırıyordu → agent playout sırasında **noise floor dondurularak** çözüldü (V8/V9). Kapsam dışı (bilinçli): kadans→2.2.1, codec/8kHz→2.2.2, tam AEC/echo→2.2.4 (burada yalnız echo-guard kancası), streaming STT connector→2.2.5, streaming TTS/ölü-hava egress→2.2.6, barge-in TTS kesme+flush→2.2.7 (burada yalnız ALGILAMA), native WebRTC VAD/DSP→SAD §21+F1 kod. Vendor-neutral (ADR-002); ham ses payload/transkript/sır repoya yazılmadı. İz: FR-RTC-004/013 + FR-RES-009 → SR-RTC-004/013 + SR-RES-009 → TC-RTC-004/013; ADR-005. → `reports/2.2.3-edge-vad-endpointing-raporu.md`
- [?] **2.2.4** Gürültü/echo işleme + agent kendi sesini tekrar transkribe etmeme — `F1` · `Must` · →FR-RTC-005/006
- [?] **2.2.5** Streaming STT connector (gateway↔orchestrator) — `F1` · `Must` · →FR-STT-002
- [?] **2.2.6** Streaming TTS connector + ölü hava işlememe (dead air) — `F1` · `Must` · →FR-RES-009
- [?] **2.2.7** Barge-in olay üretimi + TTS kesme (≤200ms) — `F1` · `Must` · →FR-RTC-002, NFR 10.1
- [?] **2.2.8** Konuşma hızı/ses seviyesi/bekleme ayarları — `F1` · `Must` · →FR-RTC-008

---

## 3. Conversation Orchestrator (Çekirdek IP)

### 3.1 Çekirdek runtime
- [?] **3.1.1** Asenkron olay döngüsü + oturum = hafif task modeli (thread-per-call yok) — `F1` · `Must` · →FR-RES-001, ADR-003
- [?] **3.1.2** Turn state machine (LISTEN→CAPTURE→THINK→ACT→SPEAK) — `F1` · `Must` · →SAD §6.1
- [x] **3.1.3** Turn-taking + barge-in koordinasyonu — `F1` · `Must` · →FR-RTC-002/003 · ⇒ `runtime/turn-taking/` (Conversation Orchestrator **Turn Manager** — SAD §6.2; orchestrator-tarafı **turn-taking + barge-in KOORDİNASYONU**; turn state machine'i SAD §6.1 sürer, edge'den (2.2.3) gelen **onaylı** turn-event akışını işler; çekirdek runtime 3.1.x için yeni `runtime/` üst dizini; 2.2.3 edge-vad disipliniyle birebir: vendor-neutral ADR-002 / credential-free / stdlib-only / **deterministik** [olay-tetikli, sanal saat; random YOK]): `turn-taking-spec.json` (kaynak doğruluk: **placement** [in_orchestrator+event_driven+non_blocking+raw_audio_in_orchestrator=false C8] + **turn_state_machine** [7 durum + legal_transitions kümesi + floor_by_state; **CAPTURE→SPEAK kasıtlı legal DEĞİL**] + **floor_control** [single_floor + agent_yields_on_barge_in + agent_speaks_only_with_floor] + **barge_in** [orch_coordination_budget_ms ≤50 green ≤20 + cancel_idempotent + **composition** edge(≈90)+orch+egress(≈40) ≤200ms NFR 10.1/SAD §20] + **backchannel** [default_yields_floor=false FR-RTC-007] + **no_spurious_interim** [suppress_response_while_user_floor + respond_only_after_endpoint FR-RTC-003] + **gates** [C1 işlenmeyen-barge-in=0 + C2 koordinasyon P95 ≤50ms + C3 zemin-ihlali=0 + C4 backchannel-eylemi=0 + C5 missed/spurious-cancel=0 + C6 illegal-geçiş=0] + **metrics** [barge_in_count → BRD §15 → observability 0.4.7 barge_in_total; call_id metrik label OLMAZ] + error_taxonomy→API §11.6 + residency NFR 10.7 + pii [ham ses payload/transkript YOK, yalnız olay tipi+sanal zaman] + **invariant C1–C10**) + `turn-taking.md` (tasarım: kapsam ayrımı [algılama 2.2.3 / kesme-flush 2.2.7 / SM-tanımı 3.1.2 / **koordinasyon bu**], model, HARD kapılar, izlenebilirlik) + `turn_taking_probe.py` (stdlib-only: `validate` C1–C10+state-machine+config+sır / `simulate <sample>` **deterministik olay-tetikli Turn Manager** [legal-geçiş kontrollü durum makinesi + tek-zemin türetme + barge-in→cancel+CAPTURE koordinasyon dispatch + idempotent yoksama + backchannel politika + erken-yanıt bastırma + lineer-interp P95 percentile 0.3.x/2.2.x ile birebir + HARD kapı→çıkış kodu] / `selftest` / `schema`) + `config/turn-taking-profiles.json` (3 profil: managed-ws-default [dispatch 12ms green] · byoc-rtp-default [18ms sarı] · low-latency [8ms]; bölge pini `${ENV}`; sır yok) + `samples/` (happy-path[tool dahil]/barge-in-speak[SPEAK→CAPTURE P95 12ms]/barge-in-think[üretim iptal]/backchannel-ignored[agent kesilmez]/no-spurious-interim[erken yanıt bastırılır]/transfer[handoff legal] geçer · turn-degraded[floor_guard+coordinate kapalı → C3+C1+C5+C6 eler] bilinçli, FR-TST-008) + `tests/turn_taking_behavior_test.py` (T1 tam-tur/T2 barge-in-SPEAK+composed/T3 barge-in-THINK/T4 idempotent/T5 backchannel-ayrımı/T6 ara-yanıt-bastırma/T7 legal-geçiş+handoff/T8 degrade) + `run_live_test.sh` (statik + sample; `${ORCHESTRATOR_URL}` varsa canlı not, yoksa SKIP) + README. **validate 52/52** 🟢 + **selftest 42/42** 🟢 (negatif kapı kanıtı: single_floor-kapalı C1/C3 / agent_yields-kapalı C1 / backchannel-default-yields C4 / koordinasyon->200ms C2 / green>bütçe C2 / max_floor_violation>0 C3 / max_illegal_transition>0 C6 / CAPTURE→SPEAK-legal-eklenir C6 / respond_only_after_endpoint-kapalı C7 / taksonomi-dışı C10 / ham-payload-yasak-kapalı C10 / literal-secret C10 → validate eler; bilinmeyen-olay + zaman-damgasız + boş-akış → simulate reddi) + **turn_taking_behavior 20/20** 🟢 + 7 sample beklendiği gibi (6 pass + degraded eler). **Bulgu:** (1) barge-in üç katmana ayrılır [algılama→koordinasyon→kesme]; orchestrator yalnız **onaylı** olayda karar verir, ham onset kesmez (edge debounce'a güvenir) → 200ms bütçe sahiplere bölünür, orkestratör dilimi ≤50ms; (2) **tek-zemin** invariant'ı turn-taking çekirdeği — `CAPTURE→SPEAK` legal değil → kullanıcının üstüne konuşma hem C3 hem C6 ile yakalanır (degraded kanıtlar); (3) **backchannel ≠ barge-in** politika kararı: "evet/hı hı" zemini almaz (FR-RTC-007+FR-RTC-003 dengesi), ayrım edge+STT semantiğinde; (4) idempotent iptal kuyruk güvenliği (geç/tekrar barge_in → tek cancel, double-flush yarışı önlenir). Kapsam dışı (bilinçli): edge VAD/barge-in algılama→2.2.3, TTS kesme/egress flush→2.2.7+2.2.1 J11, turn state machine tanımı→3.1.2, async olay döngüsü/oturum aktörü→3.1.1, per-call bütçe→3.1.4, correlation_id propagation→3.1.5, native async runtime→SAD §6.3+F1 kod. Vendor-neutral (ADR-002); ham ses payload/transkript/sır repoya yazılmadı. İz: FR-RTC-002/003/007 → SR-RTC-002/003/007 → TC-RTC-002/003/007 (RTM'de zaten eşli; SRS/RTM değişikliği gerekmedi); ADR-005/009. → `reports/3.1.3-turn-taking-barge-in-koordinasyonu-raporu.md`
- [x] **3.1.4** Per-call kaynak bütçesi (~15MB) izleme + sınırlama — `F1` · `Must` · →FR-RES-016 · ⇒ `runtime/resource-budget/` (Conversation Orchestrator oturum aktörü **Resource Budget Guard** — SAD §6.3 + Resource Manager **Cost/Resource Meter** SAD §15.2; her CANLI çağrının ayak izini ömrü boyunca **İZLER** + aşımda **kontrollü/graceful SINIRLAMA** uygular; **0.3.3 density'nin RUNTIME karşılığı** [0.3.3 offline ÖLÇÜM=bütçe ulaşılabilir mi; 3.1.4 runtime İZLEME+SINIRLAMA=aşarsa ne olur], 0.3.3 bileşen kompozisyonu birebir; 3.1.3 `turn-taking/` disipliniyle birebir: vendor-neutral ADR-002 / credential-free / stdlib-only / **deterministik** [örnek-tetikli, sanal saat; random YOK]): `resource-budget-spec.json` (kaynak doğruluk: **placement** [in_session_actor + event_driven + non_blocking + **media_in_budget=false** B8 + reports_to_resource_manager] + **budget** [mem_budget ≤15MB/soft 12MB/cpu 100mcore — **medya HARİÇ**; 6 bileşen session_state/dialogue_memory/prompt_context/rag_context/adapter_state/runtime_overhead; mitigable={dialogue,rag} FR-RES-010; grace_samples 2] + **bands** [OK ≤soft / WARN soft<x≤hard / BREACH >hard] + **enforcement** [WARN→azaltma özetle+kırp; BREACH→zorla-azalt→grace sonrası **kontrollü shed/handoff**; **graceful_only + crash_forbidden** FR-RES-014; summarize_factor %45; adillik] + **monitoring** [her oturum örneklenir, aşım tespit+yayılır FR-RES-016 'aşımı izlenir'] + **gates** [B2 kontrolsüz-aşım=0 + B3 kontrolsüz-OOM=0 + B4 tespitsiz-aşım=0 + B5 etkisiz-azaltma=0 + B7 spurious=0 adillik + B8 media_in_budget=false] + **metrics** [per_call_memory_mb/per_call_cpu_mcore/budget_breach_total → BRD §15 → observability 0.4.7; call_id metrik label OLMAZ] + error_taxonomy→API §11.6 [kontrollü-shed→QUOTA_EXCEEDED] + residency NFR 10.7 + pii [bileşen boyutu MB/mcore + sanal zaman; ham payload/transkript YOK] + **invariant B1–B10**) + `resource-budget.md` (tasarım: 0.3.3 ayrım tablosu, üç-bantlı merdiven, HARD kapı tablosu, Guard modeli, izlenebilirlik) + `resource_budget_probe.py` (stdlib-only: `validate` B1–B10+config+sır / `simulate <sample>` **deterministik BudgetGuard** [bant türetme + özetleme/kırpma azaltması + grace sonrası kontrollü shed + izleme/aşım muhasebesi + medya-hariç + lineer-interp percentile 0.3.x/3.1.3 ile birebir + HARD kapı→çıkış kodu] / `selftest` / `schema`) + `config/resource-budget-profiles.json` (3 profil: pilot-default [15/12MB NFR 10.2 birebir] · enterprise-tight [14/11MB yüksek-yoğunluk] · regulated-conservative [15/12.5MB geniş grace+handoff]; tüm mem_budget ≤15; bölge pini `${ENV}`; sır yok) + `samples/` (happy-path[≤soft eylem yok]/warn-mitigated[özetleme bütçeye döndürür 13.4→9.6MB]/breach-shed[azaltma yetmez→kontrollü graceful shed]/media-excluded[12MB medya bütçeye girmez B8] geçer · degraded[enforce+monitor kapalı → B2+B4 eler] bilinçli, FR-TST-008) + `tests/resource_budget_behavior_test.py` (T1 healthy/T2 warn-mitigated/T3 breach-shed/T4 monitoring/T5 media-excluded/T6 cpu-breach/T7 fairness/T8 determinizm+reddet) + `run_live_test.sh` (statik + sample; `${ORCHESTRATOR_URL}` varsa canlı not, yoksa SKIP) + README. **validate 56/56** 🟢 + **selftest 50/50** 🟢 (negatif kapı kanıtı: mem_budget>15 / soft≥hard / media_excluded=false / crash_forbidden=false / graceful_only=false / her gate max_*>0 / mitigable yanlış küme / taksonomi-dışı / ham-payload-yasak kapalı / literal-secret → validate eler; enforce-kapalı→kontrolsüz-aşım / monitor-kapalı→tespitsiz / on_breach=crash→kontrolsüz-OOM / spurious→adillik-ihlali; bilinmeyen-bileşen + zaman-damgasız + boş-components + negatif-boyut + boş-akış → reddi) + **behavior 23/23** 🟢 + 5 sample beklendiği gibi (4 pass + degraded eler) + run_live_test exit 0. **Bulgu:** (1) **izleme≠ölçüm**: 0.3.3 fleet density'yi ÖLÇER (offline), 3.1.4 her oturumu İZLER+SINIRLAR (runtime) — aynı kompozisyon, farklı düzlem; FR-RES-016 'aşımı izlenir' bu ayrım olmadan karşılanmaz; (2) sınırlama **üç-bantlı merdiven**: soft erken-uyarı→özetleme (çoğu uzun oturum WARN'da bütçeye döner, shed gerekmez), kontrollü shed yalnız azaltma yetmezse **son çare** + **graceful** (handoff, asla OOM-kill) → FR-RES-014'ü oturum düzeyinde somutlaştırır; (3) **medya hariçliği** bütçeyi medya konumundan bağımsız kılar (B8): ADR-009 hibritte 12MB medyaya rağmen bütçe 9.9MB; (4) **adillik (B7) bir kapı**: bütçe içindeki sağlıklı oturuma gereksiz özetleme kaliteyi düşürür → spurious_action=0; (5) grace_samples anlık tek-BREACH'i sönümler. Kapsam dışı (bilinçli): density offline ölçüm→0.3.3, worker düzeyi backpressure/admission yeni-çağrı reddi→FR-RES-014 (0.4.8), özetleme motoru→3.2.x, correlation_id propagation→3.1.5, native async runtime→SAD §6.3+F1 kod. Vendor-neutral (ADR-002); sır/ham ses payload/transkript/PII repoya yazılmadı. İz: FR-RES-016 → SR-RES-016 → TC-RES-016 (RTM'de zaten eşli; SRS/RTM değişikliği gerekmedi); ADR-003/008/009. → `reports/3.1.4-per-call-kaynak-butcesi-raporu.md`
- [x] **3.1.5** correlation_id + tenant context propagation (her olay/log/trace) — `F1` · `Must` · →SAD §13.3, §17.1 · ⇒ `runtime/context-propagation/` (Conversation Orchestrator **Context Propagator** — SAD §13.3 Tenant Context Propagation + SAD §17.1 İzleme Omurgası runtime karşılığı; çağrı **ingress**'inde oturum bağlamı (`tenant_id`+`org_unit`+`agent_id`+`correlation_id`+`region`+`call_id`+`session_id`+`trace_id`) **bir kez** kurulur ve üretilen **her** kayda — olay/log/trace span/metrik exemplar/giden adapter·tool çağrısı — **değişmez** biçimde taşınır; **0.4.7 gözlemlenebilirlik omurgası** `label_policy`'sinin **PRODUCER** tarafı [0.4.7 tüketici/collector guard, 3.1.5 üretici runtime] + **1.1.8 event-envelope** zorunlu `tenant_id`+`correlation_id` üreticisi; 3.1.3/3.1.4 disipliniyle birebir: vendor-neutral ADR-002 / credential-free / stdlib-only / **deterministik** [kayıt-tetikli, sanal saat; random YOK]): `context-propagation-spec.json` (kaynak doğruluk: **placement** [in_orchestrator+established_at_ingress+event_driven+non_blocking+immutable_binding] + **context_model** [required_keys⊇{tenant_id,correlation_id} + full_context + immutable_keys⊇{tenant_id,correlation_id,call_id} + adapter_call_required⊇{correlation_id,tenant_id,region} + single_correlation_id_per_call] + **propagation_surfaces** [event/log/span/metric/adapter_call/tool_call; metric yalnız exemplar] + **cardinality_policy** [**0.4.7 ile BİREBİR** metric_labels_allowed/forbidden/high_cardinality/exemplar — yüksek-kardinalite kimlik metrik label OLAMAZ→exemplar, TSDB koruması] + **pii_policy** [forbidden_keys + label/attribute yasağı, FR-REC-004] + **trace_propagation** [W3C traceparent + trace_id çağrı-boyu-sabit + correlation_id exemplar] + **residency** [adapter çağrıları home-region pinli, NFR 10.7] + **gates** [G1 missing_context=0 + G2 cardinality=0 + G3 pii=0 + G4 cross_tenant=0 + G5 correlation_discontinuity=0 + G6 adapter_context_missing=0 + G7 region=0 + G8 illegal_mutation=0] + **metrics** [→0.4.7 label_policy] + error_taxonomy→API §11.6 [missing→INVALID_REQUEST, cross-tenant→AUTH, region→REGION_VIOLATION] + pii [ham payload/transkript/PII DEĞERİ yok — yalnız anahtar ADLARI] + **invariant G1–G10**) + `context-propagation.md` (tasarım: mimari konum, bağlam modeli, kardinalite/PII/residency, HARD kapı tablosu, izlenebilirlik) + `context_propagation_probe.py` (stdlib-only: `validate` G1–G10+config+**0.4.7 çapraz-tutarlılık**+sır / `simulate <sample>` **deterministik ContextPropagator** [ingress bağlamı + emisyon-akışı → iliştirme + enforcement + metrik + HARD kapı→çıkış kodu] / `selftest` / `schema`) + `config/context-propagation-profiles.json` (3 profil: eu-shared/tr-shared/dedicated-regulated; region pini `${ENV}`; loglama sampling FR-RES-012; sır/PII yok) + `samples/` (ctx-happy-path/ctx-cardinality-exemplar[correlation_id→exemplar]/ctx-adapter-region-pin[farklı region pin engeller] geçer · ctx-degraded[attach/enforce/bind/pin kapalı → G1+G2+G3+G4+G6+G7+G8 eler] bilinçli, FR-TST-008) + `tests/context_propagation_behavior_test.py` (T1 bağlam-tamlığı/T2 kardinalite-exemplar/T3 PII-label+attr/T4 tenant-binding/T5 correlation-sürekliliği/T6 adapter-bağlamı/T7 region-pin/T8 reddetme+determinizm) + `run_live_test.sh` (statik + sample; `${ORCHESTRATOR_URL}` varsa canlı not, yoksa SKIP) + README. **validate 54/54** 🟢 + **selftest 41/41** 🟢 (negatif kapı kanıtı: required'tan correlation_id düşürme / izinli label'a call_id ekleme / forbidden'dan correlation_id düşürme / PII listesine izinli label ekleme kesişim / her gate>0 / immutable_binding kapatma / taksonomi-dışı / pii_values yasağı kapatma / literal secret → validate eler; attach/enforce/bind/pin politika bayrakları → ilgili gate eler; bilinmeyen yüzey + zaman-damgasız + boş-akış + ingress correlation_id eksik → reddi) + **behavior 19/19** 🟢 + 4 sample beklendiği gibi (3 pass + degraded eler) + run_live_test exit 0. **Bulgu:** (1) **producer≠consumer ama birebir**: 0.4.7 collector PII/kardinalite guard tüketici, 3.1.5 kayıt-üreten runtime; validate kardinalite listelerini 0.4.7 spec'inden **okuyarak** hizayı zorlar (probe sabiti değil → tek kaynak doğruluk); (2) **kardinalite "yeniden-konumlandırma"**: correlation_id atılmaz, metrik label'dan çıkarılıp exemplar'a bağlanır (TSDB korunur + metrik↔trace bağı kalır); (3) **tenant binding runtime invariant'ı, RLS'den ayrı**: immutable tenant_id/correlation_id/call_id ile cross-tenant **kaydı üretmeyi** baştan engeller (blocked_mutations), defense-in-depth; (4) **tek correlation_id/çağrı** trace omurgasının çekirdeği (distinct>1 → maliyet/audit izi bölünür); (5) PII anahtar-adı düzeyinde ikinci katman (asıl redaction L7). Kapsam dışı (bilinçli): kardinalite/PII/alarm kataloğu+collector guard→0.4.7, event envelope şeması→1.1.8, redaction motoru L7→3.3.x, RLS veri-katmanı→1.1.x/1.2.1, native async runtime+OTel SDK→SAD §6.3+F1 kod. Vendor-neutral (ADR-002); sır/ham ses payload'ı/transkript/PII DEĞERİ repoya yazılmadı. İz: FR-TEN-002→SR-TEN-002→TC-TEN-002 + FR-TOOL-010→SR-TOOL-010→TC-TOOL-010 + FR-BIL-002→SR-BIL-002→TC-BIL-002 + FR-RES-012→SR-RES-012→TC-RES-012 (RTM'de zaten eşli; SRS/RTM değişikliği gerekmedi); SAD §13.3/§17.1 + observability 0.4.7 + eventstream 1.1.8 + API §11.1/§11.6/§12.2; ADR-001/002/003. → `reports/3.1.5-correlation-id-tenant-context-propagation-raporu.md`

### 3.2 Session memory & prompt
- [x] **3.2.1** Kısa süreli diyalog belleği + oturum sonu kalıcılaştırma — `F1` · `Must` · →SAD §6.2 · ⇒ `runtime/session-memory/` (Conversation Orchestrator oturum aktörü **Session Memory** bileşeni — SAD §6.2: *"Kısa süreli diyalog belleği, özetleme — bellekte tutar, oturum sonunda kalıcılaştırılır"*; iki çekirdek sorumluluk: **(a) kısa diyalog belleği** [turn'leri SINIRLI pencerede tut — append-only + monoton seq + idempotent; taşma → ÖZETE katla (fold), düşürme] + **(b) oturum sonu kalıcılaştırma** [her bitişte durable yaz → ephemeral temizle]; 3.1.3/3.1.4/3.1.5 `runtime/` disipliniyle birebir: vendor-neutral ADR-002 / credential-free / stdlib-only / **deterministik** [olay-tetikli, sanal saat; random YOK]; komşu depo seam'lerini TÜKETİR — Redis çalışan kopya cache/ 1.1.5 [`session_state`/`session_summary`, `delete_on_call_end`], durable transcript db/ 1.1.3 [`transcript`/`transcript_segment`, RLS, `redaction_state='pending'`], recording objstore/ 1.1.6, olay eventstream/ 1.1.8 — motorlarını UYGULAMAZ): `session-memory-spec.json` (kaynak doğruluk: **placement** [in_orchestrator+in_session_actor+event_driven+non_blocking+persist_at_session_end + working/durable store] + **memory_model** [window_max_turns + token_soft_limit özetleme tetiği FR-LLM-005/FR-RES-010 + append_only+monotonic_seq+idempotent_append + fold_on_overflow+fold_preserves_content + speakers{caller,agent,human} + budget_component=dialogue_memory→3.1.4] + **persistence** [end_states{normal,transfer,error,abandon} + durable_targets{transcript,segment,recording,event} + persist_all_captured_turns + **persist_before_delete** + idempotent_persist+at_least_once_retry + redaction_state='pending' FR-REC-004 + strip_card_otp FR-REC-005] + **cleanup** [delete_ephemeral_after_persist + no_standing_memory + keep_on_persist_failure→TTL retry] + **summarization_hook** [fold_target=session_summary, engine→3.2.2] + **gates** [M1 sınırsız-bellek=0 + M2 out_of_order=0/duplicate dedupe + M3 unpersisted_on_end=0+persist_before_delete + M4 leaked_session_key=0 + M5 dropped=0/persisted==captured + M6 sensitive_cleartext_persisted=0+pending + M7 cross_tenant=0] + **metrics** [session_turn_count/persist_total/dropped_turn_total → BRD §15 → observability 0.4.7; call_id metrik label OLMAZ] + error_taxonomy→API §11.6 [depo-down→UNAVAILABLE/retry, kayıp değil] + residency NFR 10.7 + pii [ham metin/transkript METNİ/PII DEĞERİ YOK — yalnız turn yapısı id/speaker/seq/approx_tokens + sanal zaman + sensitive_token SAYISI] + **invariant M1–M10**) + `session-memory.md` (tasarım: mimari konum, bellek modeli, oturum-sonu persist akışı, redaction sınırı, HARD kapı tablosu, izlenebilirlik + kapsam ayrımı tablosu) + `session_memory_probe.py` (stdlib-only: `validate` M1–M10+config+sır / `simulate <sample>` **deterministik SessionMemory** [sınırlı pencere + fold + idempotent append + monoton sıra + oturum-sonu durable persist + persist_before_delete + at-least-once retry + kart/OTP strip + temizlik + HARD kapı→çıkış kodu] / `selftest` / `schema`) + `config/session-memory-profiles.json` (3 profil: pilot-default [24 turn/3000 token] · enterprise-long-call [40/5000] · high-density-tight [16/2000]; bölge pini `${ENV}`; sır yok) + `samples/` (mem-happy-path[pencere içinde→persist+temizlik]/mem-windowed-fold[token taşması→4 turn katlanır ama 12/12 kalıcılaşır kayıpsız]/mem-idempotent-replay[çift turn_id→tek kayıt + 2 retry at-least-once]/mem-end-transfer[transfer bitişi+kart/OTP işareti düz-metin yazılmaz] geçer · mem-degraded[persist+cleanup kapalı → M3+M5 eler] bilinçli FR-TST-008) + `tests/session_memory_behavior_test.py` (T1 happy/T2 windowed-fold/T3 idempotent/T4 retry/T5 4-end-state/T6 redaction/T7 cleanup-order/T8 determinizm+reddet) + `run_live_test.sh` (statik + sample; `${ORCHESTRATOR_URL}` varsa canlı not, yoksa SKIP) + README. **validate 57/57** 🟢 + **selftest 53/53** 🟢 (negatif kapı kanıtı: window_max=0 / idempotent_append=false / fold_preserves_content=false / persist_before_delete=false / redaction≠pending / strip_card_otp=false / delete_ephemeral=false / her gate>0 / taksonomi-dışı / pii-değer-yasak kapalı / literal-secret → validate eler; persist-kapalı→unpersisted+kayıp / cleanup-kapalı→kaçak / delete-first→persist_before_delete ihlali / bound-kapalı→sınırsız / no-strip→düz-metin / cross-tenant→sızıntı; geçersiz speaker/turn_id'siz/end'siz/out-of-order/bilinmeyen-olay/boş → reddi) + **behavior 26/26** 🟢 + 5 sample beklendiği gibi (4 pass + degraded eler) + run_live_test exit 0. **Bulgu:** (1) **katlama ≠ kayıp**: pencere taşması yalnız PROMPT bağlamını küçültür (token tasarrufu FR-RES-010); orijinal turn'ler oturum sonunda kalıcı kayda TAM yazılır → `persisted_turns==captured_turns` invariant'ı (M5/M9) özetleme ile dayanıklılığı ayırır; (2) **persist_before_delete kritik sıralama**: durable yazım BAŞARILI olmadan ephemeral `session_state` silinmez (M3/M4) → çökme/kesinti TTL penceresinde kurtarılabilir, sessiz kayıp engellenir; persist başarısızsa anahtar TUTULUR (depo-down→UNAVAILABLE/retry, HATA değil dayanıklılık); (3) **oturum-sonu persist, her-tur değil**: sıcak yolu (NFR 10.1) ve density'yi (NFR 10.2) korumak için durable yazım çağrı SONUNDA tek seferde (her tur PostgreSQL yazımı gecikme/density bozardı), çalışan kopya Redis aynası kurtarma sağlar; (4) **working memory PII içerir ama durable redaction_state='pending'** + kart/OTP düz-metin HİÇ yazılmaz (M6, FR-REC-004/005) — bellek katmanı içerik yorumlamaz, redaction L7 hattına devreder. Kapsam dışı (bilinçli): token-sınırı özetleme MOTORU→3.2.2, versiyonlu prompt enjeksiyonu→3.2.3, per-call bütçe izleme/sınırlama→3.1.4 (dialogue_memory besler), Redis keyspace/TTL→1.1.5, transcript şeması/RLS→1.1.3, PII redaction L7→3.3.x, correlation_id propagation→3.1.5, native async runtime→SAD §6.3+F1 kod. Vendor-neutral (ADR-002); sır/PII/ham metin/transkript repoya yazılmadı. İz: FR-RES-010→SR-RES-010→TC-RES-010 + FR-RES-016→SR-RES-016→TC-RES-016 + FR-LLM-005→SR-LLM-005→TC-LLM-005 + FR-TEN-002→SR-TEN-002→TC-TEN-002 (RTM'de zaten eşli; SRS/RTM değişikliği gerekmedi) + FR-REC-004/005/006; SAD §6.2/§6.3/§12.1; ADR-001/002/003. → `reports/3.2.1-kisa-sureli-diyalog-bellegi-oturum-sonu-kalicilastirma-raporu.md`
- [x] **3.2.2** Token sınırına göre konuşma geçmişi özetleme — `F1` · `Must` · →FR-LLM-005, FR-RES-010 · ⇒ `runtime/summarization/` (Conversation Orchestrator oturum aktörü **Session Memory ÖZETLEME MOTORU** — SAD §6.2: *"Token sınırına gelince özetler — FR-LLM-005"*; **3.2.1'in `summarization_hook` SEAM'inin ERTELEDİĞİ motor**: konuşma geçmişinin [yuvarlanan özet + aktif turn penceresi] toplam token'ı yapılandırılan TAVANI [history_token_budget] aşınca, en eski turn'leri YUVARLANAN bir özete SIKIŞTIRARAK [LlmAdapter SPI **küçük tier** — SAD §9/ADR-008] token tüketimini DÜŞÜRÜR [FR-RES-010] ama bağlamı/anlamı KORUR [FR-LLM-005 — *kırpma değil*]; 3.2.1/3.1.4/3.1.5 `runtime/` disipliniyle birebir: vendor-neutral ADR-002 / credential-free / stdlib-only / **deterministik** [olay-tetikli, sanal saat; gerçek LLM çağrısı YOK — token-muhasebe + sıkıştırma MODELİ; random YOK]; komşu seam'leri TÜKETİR — 3.2.1 hook [fold_target=session_summary] + 3.1.4 WARN-bandı 'özetle' mitigasyonu [summarize_factor] + SAD §9 LLM Router küçük tier — motorlarını UYGULAMAZ): `summarization-spec.json` (kaynak doğruluk: **placement** [in_orchestrator+in_session_actor+event_driven+non_blocking+**runs_between_turns+off_hot_path** S5+summarizer_tier=small+plugs_into_hook=3.2.1 S9] + **token_model** [history_token_budget 3000 + summary_token_cap 600 <budget + coverage_floor 0.9 + compression_ratio 0.2 + summary_retention 0.95; turn={turn_id,speaker,seq,approx_tokens,**salient_units**,t}] + **trigger** [history_tokens_over_budget + **no_spurious_under_budget** S1 + keep_at_least_one_turn] + **reduction** [history_within_budget_after_trigger S2/FR-RES-010] + **rolling_summary** [bounded+recompress_on_cap S3 + **durable_record_is_source_of_truth** — özet değil, 3.2.1 M5] + **coverage** [**summarize_not_truncate** S4 + every_folded_turn_summarized + coverage_floor] + **latency** [summarize_blocks_turn=false + summarizer_uses_small_tier + budget 200ms] + **gates** [S1 spurious=0 + S2 budget_exceeded_after=0 + S3 summary_overflow=0 + S4 uncovered_fold=0+coverage≥floor+summarized==folded + S5 blocking=0+latency≤budget + S6 double_summarize=0 + S7 cross_tenant=0 + S8 sensitive_cleartext_summarized=0] + **metrics** [summarization_total/tokens_saved/coverage_ratio → BRD §15 → 0.4.7; call_id metrik label OLMAZ] + error_taxonomy→API §11.6 [özetleyici-down→UNAVAILABLE/atla — KAYIP değil, durable 3.2.1 tam] + residency NFR 10.7 + **no_train** FR-LLM-012 + pii [ham metin/transkript METNİ/PII DEĞERİ YOK — yalnız token + **salient-birim** sayıları + sanal zaman] + **invariant S1–S10**) + `summarization.md` (tasarım: 3.2.1 seam bağı, özet≠kırpma S4 kalbi, yuvarlanan kayıpsızlık S3↔M5, sıcak yol S5, HARD kapı tablosu, izlenebilirlik) + `summarization_probe.py` (stdlib-only: `validate` S1–S10+config+sır / `simulate <sample>` **deterministik SummarizationEngine** [history-token muhasebe + tavan-tetiği FIFO katlama + yuvarlanan özet sıkıştırma + cap'te özyinelemeli re-compress + coverage [salient korunan/toplam] + off-path küçük-tier gecikme + idempotent/cross-tenant/kart-OTP-strip + HARD kapı→çıkış kodu] / `selftest` / `schema`) + `config/summarization-profiles.json` (3 profil: pilot-default [3000/600/0.9] · enterprise-long-call [5000/1000/0.92] · high-density-tight [2000/400/0.88 agresif]; bölge pini `${ENV}`; sır yok) + `samples/` (sum-happy-path[tavan altında→özetleme yok S1]/sum-token-triggered[4000>3000→tetik, history≤tavan, 1000 tok tasarruf, coverage 0.984]/sum-rolling-recompress[5400 tok→6 re-compress, özet 400/400 sabit, 3198 tok tasarruf, coverage 0.957]/sum-budget-mitigation[3.1.4 WARN-bandı bağı, 7000→5000] geçer · sum-degraded[**truncate_instead** → uncovered_fold>0+coverage 0.75 — kırpma=bilgi kaybı S4 eler] bilinçli FR-TST-008) + `tests/summarization_behavior_test.py` (T1 under-budget/T2 token-triggered/T3 bounded-recompress/T4 coverage/T5 truncate-degrade/T6 latency-off-path/T7 redaction-sensitive/T8 determinizm+reddet) + `run_live_test.sh` (statik + sample; `${ORCHESTRATOR_URL}` varsa canlı not, yoksa SKIP) + README. **validate 63/63** 🟢 + **selftest 55/55** 🟢 (negatif kapı kanıtı: history_budget=0 / summary_cap≥budget / compression_ratio≥1 / summarize_not_truncate=false / recompress_on_cap=false / summarize_blocks_turn=true / small_tier=false / her gate>0 / no_train_required=false / taksonomi-dışı / pii-değer-yasak kapalı / literal-secret → validate eler; summarize-kapalı→tavan-aşımı S2 / truncate→uncovered+coverage<floor S4 / unbounded→cap-aşımı S3 / eager→spurious S1 / blocks_turn→sıcak-yol-gecikme S5 / no-strip→kart-OTP-özete S8 / cross-tenant S7; geçersiz speaker/turn_id'siz/approx_tokens'sız/end'siz/out-of-order/boş → reddi) + **behavior 29/29** 🟢 + 5 sample beklendiği gibi (4 pass + degraded eler) + run_live_test exit 0. **Bulgu:** (1) **özet ≠ kırpma motorun kalbi (S4)**: token-sınırı naif truncation'la da karşılanır ama bilgi kaybıdır; FR-LLM-005 özetleme ister → katlanan her turn özete girer [uncovered_fold=0] + salient coverage_floor üstünde [SR-LLM-005 'bağlam korunur'], degraded truncation'ı coverage 0.75 ile yakalar; (2) **yuvarlanan özet: token sıkışır, fact korunur (S3)**: re-compress token'ı cap'e sıkıştırır ama salient fact'leri korur — coverage kaybı yalnız tek-seferlik turn→özet çıkarımından, özyineleme derinliğinden DEĞİL → 6 re-compress'te bile coverage 0.957; (3) **dayanıklı kayıt kaynak doğruluk, özet değil**: özet yalnız PROMPT bağlamını küçültür [FR-RES-010], 3.2.1 M5 [persisted==captured] tüm turn'leri durable'a tam yazar → özetleme gecikme/maliyet kolu, asla veri-kaybı kolu değil [özetleyici-down→UNAVAILABLE/atla]; (4) **sıcak yol ayrımı (S5)**: özetleme yanıt turunu bloklamaz [turlar arası, küçük tier SAD §9/ADR-008, asenkron] → e2e gecikmeye [NFR 10.1] eklenmez; (5) **3.1.4 motor-mitigasyon bağı (S9)**: per-call bütçe Guard'ı WARN-bandında 'özetle' ister, 3.2.2 o azaltmanın motorudur [tokens_saved = WARN→OK kanıtı]. Kapsam dışı (bilinçli): pencere/append/persist/temizlik→3.2.1, LLM tiering/routing/semantic-cache→SAD §9, RAG top-k trimming [FR-RES-010 diğer yarısı SR-KB-011]→RAG Client SAD §10.2, versiyonlu prompt→3.2.3, per-call bütçe→3.1.4, gerçek tokenizer→adapter, PII redaction L7→3.3.x, native async runtime→SAD §6.3+F1 kod. Vendor-neutral (ADR-002); sır/PII/ham metin/transkript repoya yazılmadı. İz: FR-LLM-005→SR-LLM-005→TC-LLM-005 + FR-RES-010→SR-LLM-005/SR-RES-010→TC-LLM-005/TC-RES-010 (RTM'de WBS=3.2.2 zaten eşli; SRS/RTM değişikliği gerekmedi) + FR-RES-005/FR-LLM-012/FR-REC-004/005; SAD §6.2/§9/§20; ADR-001/002/003/008. → `reports/3.2.2-token-sinirina-gore-konusma-gecmisi-ozetleme-raporu.md`
- [ ] **3.2.3** Prompt Manager: versiyonlu sabit system prompt enjeksiyonu — `F1` · `Must` · →FR-LLM-006
- [ ] **3.2.4** Single-prompt + node/flow konuşma modelleri çalıştırma — `F1` · `Must` · →FR-AGT-003

### 3.3 Policy engine
- [ ] **3.3.1** Input guard: prompt-injection/jailbreak tespiti — `F1` · `Must` · →FR-LLM-007
- [ ] **3.3.2** Output guard: yanıt politika denetimi (BRD §13 yasak işlemler) — `F1` · `Must` · →FR-LLM-009
- [ ] **3.3.3** "No deceptive impersonation" + AI bildirimi zorlama — `F1` · `Must` · →BRD §5, §14.2
- [ ] **3.3.4** Anti-hallucination kuralları (kaynak yoksa uydurma yok → teyit/aktar/ticket) — `F1` · `Must` · →FR-KB-007
- [ ] **3.3.5** Global + tenant seviyesinde politika uygulama — `F1` · `Must` · →FR-AGT-009
- [ ] **3.3.6** Kontrollü tekrar stratejisi + N anlamama → handoff — `F1` · `Must` · →FR-RTC-010/011

### 3.4 Yerelleştirme / okuma
- [ ] **3.4.1** Sayı/tarih/para/adres/kod yerel okuma normalizasyonu — `F1` · `Must` · →FR-RTC-009
- [ ] **3.4.2** Konuşma sırasında dil değişimi — `F2` · `Should` · →FR-RTC-012

---

## 4. Sağlayıcı Soyutlama Katmanı (Adapters)

### 4.1 SPI ve ortak yetenekler
- [ ] **4.1.1** Adapter SPI tanımı (STT/TTS/LLM/Telephony) — `F1` · `Must` · →SAD §8.1
- [ ] **4.1.2** Ortak: timeout, retry (backoff+jitter), circuit breaker, health check — `F1` · `Must` · →SAD §8.2
- [ ] **4.1.3** Usage metering + cost calculation (her adapter) — `F1` · `Must` · →FR-BIL-002
- [ ] **4.1.4** Region selection + data retention control (adapter düzeyi) — `F2` · `Must` · →NFR 10.7
- [ ] **4.1.5** Provider-specific error normalization (ortak hata taksonomisi) — `F1` · `Must` · →SAD §8.2
- [ ] **4.1.6** Connection pooling + kalıcı oturumlar — `F1` · `Must` · →FR-RES-006

### 4.2 Somut adapter'lar (≥2/kategori)
- [ ] **4.2.1** STT adapter #1 + #2 (partial/final, confidence, phrase boosting) — `F1` · `Must` · →FR-STT-001/004/006
- [ ] **4.2.2** STT alan optimizasyonu (telefon no/plaka/poliçe/referans) — `F1` · `Must` · →FR-STT-005
- [ ] **4.2.3** TTS adapter #1 + #2 (streaming, pronunciation dictionary, cancel) — `F1` · `Must` · →FR-TTS-001/004/005
- [ ] **4.2.4** LLM adapter #1 + #2 (streaming token/tool call) — `F1` · `Must` · →FR-LLM-001/004
- [ ] **4.2.5** Telephony adapter #1 + #2 — `F1`/`F2` · `Must` · →FR-TEL-002
- [ ] **4.2.6** Ses klonlama izin + kullanım kaydı (onaylı sesler) — `F2` · `Should`/`Must` · →FR-TTS-006/007

### 4.3 Fallback ve degradation
- [ ] **4.3.1** STT fallback (hata/timeout → ikincil) — `F1` · `Must` · →FR-STT-008
- [ ] **4.3.2** TTS fallback + ses karakteri tutarlılığı — `F1` · `Must` · →FR-TTS-008/009
- [ ] **4.3.3** LLM fallback (fallback model / deterministic flow) — `F1` · `Must` · →FR-LLM-010
- [ ] **4.3.4** Birincil kesintide kontrollü fallback uçtan uca testi — `F1` · `Must` · →BRD §19 (4)

---

## 5. LLM Orkestrasyonu (Router · Tiering · Cache)

- [ ] **5.1** LLM Router iskeleti (cost/latency/dil/risk girdileri) — `F1` · `Must` · →FR-LLM-003
- [ ] **5.2** Tur sınıflandırıcı (rutin/karmaşık/riskli) — `F2` · `Must` · →FR-LLM-013
- [ ] **5.3** Model tiering (küçük/büyük) + ≥%60 küçük-model tur hedefi — `F2` · `Must` · →FR-RES-005, NFR 10.2
- [ ] **5.4** Semantic cache (embedding; yan-etkisiz turlar; PII cache'lenmez) — `F2` · `Should` · →FR-LLM-014, FR-RES-004
- [ ] **5.5** Tenant/use-case model override — `F2` · `Must` · →FR-LLM-002
- [ ] **5.6** Schema-validated tool çağrısı zorlaması (serbest metin yerine) — `F1` · `Must` · →FR-LLM-008
- [ ] **5.7** Model+versiyon+token kullanımını çağrı bazında kaydet — `F1` · `Must` · →FR-LLM-011
- [ ] **5.8** "Tenant verisi eğitime kapalı" varsayılanı (no-train) — `F1` · `Must` · →FR-LLM-012

---

## 6. Bilgi Tabanı & RAG

### 6.1 İndeksleme (offline)
- [ ] **6.1.1** Ingest connector: PDF/Word/HTML/metin/CSV/web — `F1` · `Must` · →FR-KB-001
- [ ] **6.1.2** SharePoint/Confluence connector — `F2` · `Should` · →FR-KB-002
- [ ] **6.1.3** Parse→chunk→embed→index + versiyonlama — `F1` · `Must` · →FR-KB-003
- [ ] **6.1.4** Doküman bazında erişim yetkisi (metadata) — `F1` · `Must` · →FR-KB-005
- [ ] **6.1.5** Bayatlama/işaretleme (content TTL) — `F2` · `Should` · →FR-KB-008

### 6.2 Retrieval (online)
- [ ] **6.2.1** Vector search (top-k) + opsiyonel rerank — `F1` · `Must` · →SAD §10.2
- [ ] **6.2.2** Token trimming (bağlam maliyeti sınırı) — `F1` · `Must` · →FR-KB-011, FR-RES-010
- [ ] **6.2.3** Kaynak atfı (yanıt hangi kaynağa dayalı) — `F1` · `Must` · →FR-KB-006
- [ ] **6.2.4** Hassas doküman → sağlayıcı loglarına gitmeme — `F1` · `Must` · →FR-KB-010
- [ ] **6.2.5** KB Q&A benchmark veri seti + test — `F2` · `Must` · →FR-KB-009

---

## 7. Tool & Kurumsal Entegrasyon

### 7.1 Tool yürütme hattı
- [ ] **7.1.1** Tool input/output JSON schema doğrulama — `F1` · `Must` · →FR-TOOL-002
- [ ] **7.1.2** Tool authorization (agent scope, read/write seviyesi) — `F1` · `Must` · →FR-TOOL-004/005
- [ ] **7.1.3** Idempotency key (duplicate işlem önleme) — `F1` · `Must` · →FR-TOOL-009
- [ ] **7.1.4** Timeout/retry/circuit breaker (Integration GW) — `F1` · `Must` · →FR-TOOL-003
- [ ] **7.1.5** Hata normalizasyonu (müşteriye teknik detay yok) — `F1` · `Must` · →FR-TOOL-008
- [ ] **7.1.6** correlation_id ile tool izleme + audit — `F1` · `Must` · →FR-TOOL-010

### 7.2 Integration Gateway
- [ ] **7.2.1** REST connector — `F1` · `Must` · →FR-TOOL-001
- [ ] **7.2.2** SOAP/GraphQL/webhook connector'lar — `F2` · `Must` · →FR-TOOL-001
- [ ] **7.2.3** Endpoint allowlist (onaysız endpoint engelleme) — `F1` · `Must` · →FR-TOOL-012
- [ ] **7.2.4** Asenkron uzun-işlem workflow (callback/polling) — `F2` · `Should` · →FR-TOOL-011
- [ ] **7.2.5** CRM/Ticketing/ERP referans entegrasyonu (pilot) — `F1` · `Must` · →BRD §6.1

### 7.3 Deterministic workflow engine
- [ ] **7.3.1** Kritik işlem workflow durum makinesi (auth→kural→özet→teyit→onay→audit) — `F1` · `Must` · →BRD §13
- [ ] **7.3.2** Kritik işlem öncesi müşteri teyidi + ek doğrulama — `F1` · `Must` · →FR-TOOL-006/007

---

## 8. Çağrı İçi Kimlik Doğrulama

- [ ] **8.1** Caller ID zayıf-sinyal eşleştirme (tek başına yetersiz) — `F1` · `Must` · →FR-AUTH-001
- [ ] **8.2** OTP / müşteri no / KBA doğrulama — `F1` · `Must` · →FR-AUTH-002
- [ ] **8.3** Step-up authentication (hassas işlem) — `F1` · `Must` · →FR-AUTH-003
- [ ] **8.4** Başarısız deneme limiti — `F1` · `Must` · →FR-AUTH-004
- [ ] **8.5** Hassas bilgi sesli tam tekrar etmeme — `F1` · `Must` · →FR-AUTH-005
- [ ] **8.6** Ses biyometrisi (opsiyonel ayrı modül, açık onay + politika) — `F3` · `Should` · →FR-AUTH-006/007

---

## 9. İnsan Temsilciye Aktarım (Handoff)

- [ ] **9.1** Cold transfer (SIP REFER) — `F1` · `Must` · →FR-TEL-007
- [ ] **9.2** Warm transfer (köprüleme + whisper brifing) — `F1` · `Must` · →FR-TEL-007, FR-HND-006
- [ ] **9.3** Whisper transfer — `F2` · `Must` · →FR-TEL-007
- [ ] **9.4** Transfer tetikleyiciler (kullanıcı isteği, düşük confidence, öfke, politika) — `F1` · `Must` · →FR-HND-001/002
- [ ] **9.5** Kuyruk/skill/departman bazlı hedef seçimi — `F2` · `Must` · →FR-TEL-008, FR-HND-003
- [ ] **9.6** Bağlam paketi (özet + intent + toplanan alanlar + auth durumu) + screen-pop — `F1` · `Must` · →FR-HND-004/005
- [ ] **9.7** Temsilci yoksa callback/voicemail/ticket — `F1` · `Must` · →FR-HND-007
- [ ] **9.8** Aktarım başarısı + bekleme süresi raporlama — `F2` · `Must` · →FR-HND-008

---

## 10. Outbound & Kampanya & Consent

### 10.1 Kampanya yönetimi
- [ ] **10.1.1** Kampanya oluşturma + müşteri listesi yükleme — `F2` · `Must` · →FR-OUT-001
- [ ] **10.1.2** CRM'den dinamik liste alma — `F2` · `Must` · →FR-OUT-002
- [ ] **10.1.3** Max deneme + yeniden arama aralığı — `F2` · `Must` · →FR-OUT-005
- [ ] **10.1.4** İnsan/voicemail/geçersiz numara ayrımı — `F2` · `Must` · →BRD §8.2
- [ ] **10.1.5** Disposition (otomatik) + voicemail/meşgul/cevapsız kayıt — `F2` · `Must` · →FR-OUT-008/011
- [ ] **10.1.6** Kampanya script/teklif versiyonu çağrı bazında kayıt — `F2` · `Must` · →FR-OUT-009
- [ ] **10.1.7** Kampanya durdurma düğmesi — `F2` · `Must` · →FR-OUT-010
- [ ] **10.1.8** A/B test kampanyaları — `F2` · `Should` · →FR-OUT-012

### 10.2 Consent & uyumluluk motoru
- [ ] **10.2.1** Consent Engine ön-kontrol (amaç/ülke/birey-şirket/kaynak/tarih/kapsam) — `F2` · `Must` · →FR-OUT-003, §14.3
- [ ] **10.2.2** Do-not-call / suppression gerçek zamanlı kontrol — `F2` · `Must` · →FR-TEL-014, FR-OUT-006
- [ ] **10.2.3** Ülke/bölge arama saati kuralları — `F2` · `Must` · →FR-TEL-013, FR-OUT-004
- [ ] **10.2.4** Silent/abandoned call önleme (kapasite kontrolü) — `F2` · `Must` · →FR-TEL-015
- [ ] **10.2.5** İYS/ETK (TR) + PECR/Ofcom (UK) profil parametreleri — `F2` · `Must` · →§14.3
- [ ] **10.2.6** Kampanya kapasitesi ≤ agent+trunk kapasitesi (backpressure entegrasyonu) — `F2` · `Must` · →FR-OUT-007

---

## 11. Kayıt, Transkript & PII Redaction

- [ ] **11.1** Kayıt politikası (tenant/ülke/use-case) + tamamen kapatma — `F1` · `Must` · →FR-REC-001/002
- [ ] **11.2** Tek/çift kanallı kayıt — `F1` · `Must` · →FR-REC-003
- [ ] **11.3** Transkript üretimi + timeline — `F1` · `Must` · →BRD §8.1
- [ ] **11.4** PII redaction pipeline (async) — `F1` · `Must` · →FR-REC-004
- [ ] **11.5** Kart/parola/OTP kayıt+transkriptten çıkarma — `F1` · `Must` · →FR-REC-005
- [ ] **11.6** Kayıt/transkript erişim audit'i — `F1` · `Must` · →FR-REC-009
- [ ] **11.7** Çağrı özeti üretimi — `F1` · `Must` · →BRD §8.1

---

## 12. IAM, RBAC & Panel Backend (AuthZ)

### 12.1 IAM çekirdeği
- [ ] **12.1.1** RBAC modeli (rol→permission-key bundle, immutable) — `F1` · `Must` · →FR-IAM-001, FR-IAM-011
- [ ] **12.1.2** Permission-key kataloğu (`kaynak:eylem`, `*:own`) — `F1` · `Must` · →SAD §14.4.3
- [ ] **12.1.3** Scoped assignment (rol + departman/marka/kampanya filtresi) — `F2` · `Must` · →FR-IAM-011, ADR-012
- [ ] **12.1.4** SSO: SAML 2.0 + OIDC — `F2` · `Must` · →FR-IAM-002
- [ ] **12.1.5** MFA — `F1` · `Must` · →FR-IAM-003
- [ ] **12.1.6** SCIM provisioning + IdP grup→rol eşleme — `F2` · `Should` · →FR-IAM-007, SAD §14.4.4
- [ ] **12.1.7** Maker-checker onay akışı — `F2` · `Should` · →FR-IAM-005
- [ ] **12.1.8** Append-only (WORM) audit log + bütünlük — `F1` · `Must` · →FR-IAM-006

### 12.2 Panel AuthZ enforcement
- [ ] **12.2.1** Backend guard: panel + rol + tenant scope (FastAPI dependency) — `F1` · `Must` · →SAD §14.4.2
- [ ] **12.2.2** L0/L1/L2 ayrı router ağaçları + ayrı OAuth scope — `F1` · `Must` · →SAD §14.4.2
- [ ] **12.2.3** Tenant scope + RLS çift kontrol — `F1` · `Must` · →FR-TEN-002
- [ ] **12.2.4** L0 iş verisi repository bağımsızlığı (tasarımsal izolasyon) — `F1` · `Must` · →FR-IAM-008

### 12.3 Break-glass (üç katmanlı)
- [ ] **12.3.1** Tier A (metrik/log, PII yok) — break-glass'sız L0 + audit — `F1` · `Must` · →FR-IAM-009
- [ ] **12.3.2** Tier B: maker-checker + time-boxed token (60dk default, max 4sa, auto-expiry) — `F2` · `Must` · →FR-IAM-009
- [ ] **12.3.3** Tier B: gerekçe kodu + tenant security_compliance_officer/tenant_owner bildirimi — `F2` · `Must` · →FR-IAM-009
- [ ] **12.3.4** Regüle tenant `require_tenant_approval` toggle + DPA bağı — `F2` · `Must` · →FR-IAM-010
- [ ] **12.3.5** Break-glass tam-audit router (ayrı, kısıtlı) — `F2` · `Must` · →SAD §14.4.2

### 12.4 Tenant & organizasyon yönetimi
- [ ] **12.4.1** Tenant CRUD + provisioning (L0) — `F1` · `Must` · →FR-TEN-001
- [ ] **12.4.2** Dedicated vs shared tenant — `F2` · `Should` · →FR-TEN-005
- [ ] **12.4.3** Org yapısı (marka/departman/ülke/proje) (L1) — `F1` · `Must` · →FR-TEN-003
- [ ] **12.4.4** Tenant dil/saat dilimi/bölge/saklama tercihi (L1) — `F1` · `Must` · →FR-TEN-004

---

## 13. Frontend Panelleri (L0 / L1 / L2)

### 13.1 Ortak frontend altyapısı
- [ ] **13.1.1** İki ayrı Next.js app: `platform-app` (L0, internal-only) + `tenant-app` (L1+L2) — `F1` · `Must` · →ADR-011
- [ ] **13.1.2** Route group + middleware (oturum + tenant scope + panel ayrımı) — `F1` · `Must` · →SAD §14.4.1
- [ ] **13.1.3** Tasarım sistemi / komponent kütüphanesi + i18n (TR/EN) — `F1` · `Must`
- [ ] **13.1.4** PII redaction/maskeleme panel görüntülemede — `F1` · `Must` · →BRD §17.7

### 13.2 L0 — Platform Admin Console (P-01..P-09)
- [ ] **13.2.1** P-01 Platform Genel Bakış — `F2` · `Must`
- [ ] **13.2.2** P-02 Tenant Yönetimi & Provisioning — `F2` · `Must`
- [ ] **13.2.3** P-03 Kaynak & Kapasite Yönetimi — `F2` · `Must` · →Resource Manager
- [ ] **13.2.4** P-04 Sağlayıcı & Entegrasyon Sağlığı — `F2` · `Must`
- [ ] **13.2.5** P-05 Platform Faturalandırma & Rate-Card — `F2` · `Must`
- [ ] **13.2.6** P-06 Global Politika & Guardrails — `F2` · `Must`
- [ ] **13.2.7** P-07 Platform Audit & Güvenlik — `F2` · `Must`
- [ ] **13.2.8** P-08 Sürüm & Dağıtım (Release) Yönetimi — `F2` · `Should`
- [ ] **13.2.9** P-09 Alarm & Incident (SRE) — `F2` · `Must`

### 13.3 L1 — Tenant Admin Console (T-01..T-09)
- [ ] **13.3.1** T-01 Tenant Dashboard — `F2` · `Must`
- [ ] **13.3.2** T-02 Organizasyon & Yapı — `F2` · `Must`
- [ ] **13.3.3** T-03 Kullanıcı & Rol Yönetimi (RBAC/SSO/SCIM) — `F2` · `Must`
- [ ] **13.3.4** T-04 Telefon Numarası & SIP/Trunk — `F2` · `Must`
- [ ] **13.3.5** T-05 Entegrasyon, Tool & API Key/Webhook — `F2` · `Must`
- [ ] **13.3.6** T-06 Compliance & Retention — `F2` · `Must`
- [ ] **13.3.7** T-07 Faturalandırma & Kullanım — `F2` · `Must`
- [ ] **13.3.8** T-08 Audit Log (tenant) — `F2` · `Must`
- [ ] **13.3.9** T-09 Kaynak Kotası Görünümü — `F2` · `Must`

### 13.4 L2 — Operasyon / Uygulama Paneli (A-01..A-17)
- [ ] **13.4.1** A-01 Operasyon Dashboard — `F1` · `Must`
- [ ] **13.4.2** A-02 Canlı Çağrılar — `F1` · `Must` · →FR-ANA-012
- [ ] **13.4.3** A-03 Agent Listesi — `F1` · `Must`
- [ ] **13.4.4** A-04 Agent Builder (kod yazmadan) — `F1` · `Must` · →FR-AGT-001
- [ ] **13.4.5** A-05 Conversation Flow Editor — `F1` · `Must` · →FR-AGT-003
- [ ] **13.4.6** A-06 Prompt Editor (versiyonlama) — `F1` · `Must` · →FR-AGT-004
- [ ] **13.4.7** A-07 Voice & Model Ayarları (tiering) — `F1` · `Must` · →FR-AGT-002
- [ ] **13.4.8** A-08 Knowledge Base — `F1` · `Must`
- [ ] **13.4.9** A-09 Tool/API Bağlama — `F1` · `Must`
- [ ] **13.4.10** A-10 Outbound Kampanya Yönetimi — `F2` · `Must`
- [ ] **13.4.11** A-11 Çağrı Kayıtları — `F1` · `Must`
- [ ] **13.4.12** A-12 Çağrı Detayı / Transkript & Timeline — `F1` · `Must`
- [ ] **13.4.13** A-13 QA Değerlendirme — `F2` · `Must`
- [ ] **13.4.14** A-14 Analytics & Raporlama — `F1`/`F2` · `Must`
- [ ] **13.4.15** A-15 Maliyet & Kaynak Tüketimi — `F2` · `Must` · →FR-ANA-013
- [ ] **13.4.16** A-16 Test & Simulation Centre — `F1` · `Must`
- [ ] **13.4.17** A-17 Sürüm Geçmişi (agent) + rollback — `F1` · `Must` · →FR-AGT-006

---

## 14. Analitik, QA & Gözlemlenebilirlik

### 14.1 Gözlemlenebilirlik
- [ ] **14.1.1** Çağrı trace span'leri (BRD §15 tüm zaman damgaları) — `F1` · `Must` · →SAD §17.1
- [ ] **14.1.2** Teknik metrikler (packet loss/jitter/STT-LLM-TTS latency/token/...) — `F1` · `Must` · →BRD §15
- [ ] **14.1.3** Per-call CPU/bellek/eşzamanlılık ölçümü — `F1` · `Must` · →FR-ANA-013
- [ ] **14.1.4** Yapılandırılmış asenkron + örneklemeli loglama — `F1` · `Should` · →FR-RES-012
- [ ] **14.1.5** Alarm kuralları (BRD §15) + ≤2dk üretim — `F1` · `Must` · →NFR 10.1
- [ ] **14.1.6** Gerçek zamanlı operasyon ekranı (≤60sn gecikme) — `F1` · `Must` · →FR-ANA-012

### 14.2 Analitik & kalite
- [ ] **14.2.1** Otomatik kalite değerlendirme (tüm çağrılar) — `F2` · `Must` · →FR-ANA-001
- [ ] **14.2.2** Intent/outcome/disposition/completion çıkarımı — `F1`/`F2` · `Must` · →FR-ANA-002
- [ ] **14.2.3** Containment/transfer oranı raporu — `F1` · `Must` · →FR-ANA-003
- [ ] **14.2.4** Yanlış bilgi/tool hatası/güvenlik ihlali tespiti — `F2` · `Must` · →FR-ANA-004
- [ ] **14.2.5** Kritik konuşma otomatik işaretleme — `F2` · `Must` · →FR-ANA-008
- [ ] **14.2.6** QA manuel skor + açıklama — `F2` · `Must` · →FR-ANA-009
- [ ] **14.2.7** Agent sürümleri performans karşılaştırma — `F2` · `Must` · →FR-ANA-010
- [ ] **14.2.8** Dashboard + ham veri export — `F2` · `Must` · →FR-ANA-011
- [ ] **14.2.9** Maliyet raporu (çağrı/agent/tenant/sağlayıcı) — `F2` · `Must` · →FR-ANA-007

---

## 15. Faturalama & Kullanım

- [ ] **15.1** Dakika/saniye bazlı kullanım ölçümü — `F2` · `Must` · →FR-BIL-001
- [ ] **15.2** Telekom/STT/TTS/LLM/platform maliyeti ayrı izleme — `F2` · `Must` · →FR-BIL-002
- [ ] **15.3** Tenant fiyat planı tanımı — `F2` · `Must` · →FR-BIL-003
- [ ] **15.4** Minimum ücret/kota/overage — `F2` · `Must` · →FR-BIL-004
- [ ] **15.5** Kullanım limiti + bütçe alarmı — `F2` · `Must` · →FR-BIL-006
- [ ] **15.6** Dedicated altyapı maliyeti ayrı faturalandırma — `F3` · `Should` · →FR-BIL-005
- [ ] **15.7** Fatura verisi finans sistemine aktarım — `F2` · `Must` · →FR-BIL-007

---

## 16. Kaynak Verimliliği & Resource Manager

- [ ] **16.1** TTS cache (statik anonslar, hit ≥%80) — `F1` · `Must` · →FR-RES-003
- [ ] **16.2** Quota Service (tenant concurrency/CPS/vCPU/bellek) — `F1`/`F2` · `Must` · →FR-TEN-006/007
- [ ] **16.3** Autoscaler + warm pool (cold-start azaltma) — `F2` · `Must` · →FR-RES-013, NFR 10.3
- [ ] **16.4** Scale-to-zero (boşta tenant/worker) — `F2` · `Must` · →FR-RES-007
- [ ] **16.5** Backpressure / admission control (graceful degradation) — `F1`/`F2` · `Must` · →FR-RES-014
- [ ] **16.6** Cost/Resource Meter → Analytics & Billing — `F2` · `Must` · →SAD §15.2
- [ ] **16.7** Non-RT işleri batch/async (analitik/redaction/raporlama) — `F1` · `Must` · →FR-RES-011
- [ ] **16.8** Noisy-neighbor önleme (fair scheduling) — `F2` · `Must` · →NFR 10.3
- [ ] **16.9** GPU yalnız self-hosted gerekince (aksi CPU/serverless) — `F3` · `Should` · →FR-RES-015, ADR-010

---

## 17. Güvenlik & Uyumluluk

### 17.1 Güvenlik
- [ ] **17.1.1** TLS her yerde + SRTP medya — `F1` · `Must` · →NFR 10.6
- [ ] **17.1.2** Depolama AES-256 + tenant başına KMS key — `F1` · `Must` · →NFR 10.6
- [ ] **17.1.3** BYOK / customer-managed keys — `F3` · `Should` · →NFR 10.6
- [ ] **17.1.4** WAF/DDoS + rate limiting + IP allowlist + private endpoint — `F1` · `Must` · →NFR 10.6
- [ ] **17.1.5** SAST/DAST/dependency scan/SBOM (CI'da zorunlu) — `F0`/`F1` · `Must` · →NFR 10.6
- [ ] **17.1.6** PAM + incident response prosedürü — `F2` · `Must` · →NFR 10.6
- [ ] **17.1.7** Düzenli penetration test (kritik bulgu yok kapısı) — `F1` · `Must` · →BRD §19 (20)

### 17.2 Uyumluluk
- [ ] **17.2.1** Veri sahibi hakları (erişim/düzeltme/silme) akışları — `F2` · `Must` · →§14.1
- [ ] **17.2.2** DPA + alt-işleyen listesi + sağlayıcıya gönderilen veri kaydı — `F2` · `Must` · →§14.1
- [ ] **17.2.3** AI şeffaflık bildirimi (yapılandırılabilir; EU AI Act 02.08.2026) — `F1` · `Must` · →§14.2
- [ ] **17.2.4** Country compliance profiles (TR/UK/EU/ME) parametre motoru — `F2` · `Must` · →§14.4
- [ ] **17.2.5** Sektörel profiller: PCI/FCA/HIPAA/NHS/SOC2/ISO27001-27701/DORA/NIS2 — `F3` · `Must` · →§14.4
- [ ] **17.2.6** PCI ödeme akışı (kart verisi LLM/transkript/kayıt dışı; DTMF masking/aktarım) — `F3` · `Must` · →BRD §8.5

---

## 18. Test, Simülasyon & NFR Doğrulama

- [ ] **18.1** Tarayıcı üzerinden agent testi (telefon olmadan) — `F1` · `Must` · →FR-TST-001
- [ ] **18.2** Persona/senaryo simülasyonu (happy/edge/adversarial) — `F1` · `Must` · →FR-TST-002/003
- [ ] **18.3** Sentetik test verisi üretimi (gerçek müşteri verisi yok) — `F1` · `Must` · →FR-TST-008
- [ ] **18.4** Prompt değişiminde otomatik regression + yayın engelleme — `F1` · `Must` · →FR-TST-004/005, FR-AGT-010
- [ ] **18.5** Yük + eş zamanlı çağrı testi (tasarım kapasitesi) — `F1`/`F2` · `Must` · →FR-TST-006
- [ ] **18.6** Yük altında per-call kaynak ölçümü + regresyon raporu — `F1` · `Must` · →FR-TST-009
- [ ] **18.7** Gürültü/aksan/kesinti/düşük hat kalitesi test seti — `F2` · `Should` · →FR-TST-007
- [ ] **18.8** Prompt injection / veri sızıntısı güvenlik testi — `F1` · `Must` · →BRD §19 (8)
- [ ] **18.9** Tenant izolasyon testi (cross-tenant erişim yok) — `F1` · `Must` · →BRD §19 (9)
- [ ] **18.10** Gecikme P95 hedef doğrulama — `F1` · `Must` · →BRD §19 (10), NFR 10.1
- [ ] **18.11** PII redaction testleri — `F1` · `Must` · →BRD §19 (13)
- [ ] **18.12** Outbound consent/opt-out doğrulama testi — `F2` · `Must` · →BRD §19 (14)

---

## 19. HA/DR & Çoklu Bölge

- [ ] **19.1** Multi-AZ (tüm stateful servisler) — `F1` · `Must` · →NFR 10.5
- [ ] **19.2** Active-passive bölgesel DR + yeni çağrı failover — `F2` · `Must` · →NFR 10.5
- [ ] **19.3** RTO doğrulama (voice ≤15dk, yönetim ≤4sa) — `F2` · `Must` · →NFR 10.5
- [ ] **19.4** DR tatbikatı (yılda ≥2) süreci — `F2` · `Must` · →NFR 10.5
- [ ] **19.5** Active-active multi-region (10.000+) — `F3` · `Must` · →NFR 10.3, BRD §20
- [ ] **19.6** Uptime SLA izleme (voice/telephony/API %99,99) — `F2` · `Must` · →NFR 10.4

---

## 20. Dağıtım, Sürümleme & Operasyon

- [ ] **20.1** Agent yaşam döngüsü: draft/test/staging/production + rollback — `F1` · `Must` · →FR-AGT-005/006
- [ ] **20.2** Agent varyasyonları (segment bazlı) — `F2` · `Should` · →FR-AGT-008
- [ ] **20.3** Feature flag + kademeli yayma (release) — `F2` · `Should` · →P-08
- [ ] **20.4** On-premise / hybrid dağıtım paketi — `F3` · `Could` · →BRD §20
- [ ] **20.5** Marketplace & partner ekosistemi — `F3` · `Could` · →BRD §20
- [ ] **20.6** Runbook'lar + on-call + incident yönetimi (P-09) — `F2` · `Must` · →BRD §15

---

## Ek A — Faz 1 kabul kapısı (BRD §19 inbound altküme)
- [ ] ≥2 STT, ≥2 TTS, ≥2 LLM sağlayıcı + fallback (→4.2/4.3)
- [ ] Inbound çağrı uçtan uca tamamlanır (→2/3)
- [ ] Warm + cold transfer çalışır (→9)
- [ ] Agent CRM'den okur, kontrollü işlem yapar (→7)
- [ ] Prompt injection veri sızdırmaz (→18.8)
- [ ] Tenant izolasyonu doğrulanır (→18.9)
- [ ] P95 gecikme hedefte (→18.10)
- [ ] PII redaction geçer (→18.11)
- [ ] Agent rollback (→20.1)
- [ ] Kritik işlemler audit'te (→12.1.8)
- [ ] Yük altında per-call kaynak bütçede + density hedefi (→18.6, 0.3.3)
- [ ] Pentest kritik bulgu yok (→17.1.7)

## Ek B — Bağımlılık notları (kritik sıra)
1. `0.x` (Foundation + PoC + ADR-009) → her şeyin önkoşulu.
2. `1.1` (şema) → `3` (orchestrator) ve `12` (IAM) önkoşulu.
3. `4` (adapters) → `3`, `5`, `6` için gerekli.
4. `2` (media) + `3` (orchestrator) + `4` (adapters) → ilk uçtan uca akış (F1 çekirdek).
5. `12.2/12.3` (panel AuthZ + break-glass) → `13` (frontend panelleri) önkoşulu.
6. `16` (Resource Manager) → `10.2.6` (outbound kapasite) ve F2 density hedefleri.
7. `10` (outbound) F2; `17.2.5/17.2.6` (PCI/sektörel) F3.

## Ek C — Bakım
- Her görev için PR'da WBS ID referansı verilir.
- Faz kapısı geçişinde bu dosya + izlenebilirlik matrisi (0.1.2) gözden geçirilir.
- Yeni gereksinim → önce BRD/SAD güncellenir, sonra buraya görev eklenir (FR-ID şeması korunur).
