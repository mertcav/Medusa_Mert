{
  "_comment": "WBS 1.1.6 — O5: tenant runtime rolüne yalnız KENDİ bucket'ında erişim. Per-tenant-per-region bucket olduğundan Resource bucket ARN'ıdır; cross-tenant erişim yapısal olarak olanaksız (FR-TEN-002). KMS izni de yalnız tenant CMK'sına. Placeholder'lar deployment'ta doldurulur.",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "TenantBucketRW",
      "Effect": "Allow",
      "Action": [
        "s3:GetObject",
        "s3:PutObject",
        "s3:ListBucket",
        "s3:GetObjectVersion"
      ],
      "Resource": [
        "arn:aws:s3:::${BUCKET}",
        "arn:aws:s3:::${BUCKET}/*"
      ],
      "Condition": {
        "Bool": { "aws:SecureTransport": "true" }
      }
    },
    {
      "Sid": "TenantCmkUseOnly",
      "_invariant": "O2 — yalnız tenant CMK ile şifrele/çöz",
      "Effect": "Allow",
      "Action": [
        "kms:Encrypt",
        "kms:Decrypt",
        "kms:GenerateDataKey"
      ],
      "Resource": "${TENANT_KMS_KEY_ARN}"
    },
    {
      "Sid": "NoDeleteOnContentByRuntime",
      "_invariant": "O8/O10 — runtime obje silmez (silme yalnız retention motoru + crypto-shred)",
      "Effect": "Deny",
      "Action": [
        "s3:DeleteObject",
        "s3:DeleteObjectVersion"
      ],
      "Resource": "arn:aws:s3:::${BUCKET}/*"
    }
  ]
}
